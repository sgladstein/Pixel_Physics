THIRD-PARTY NOTICES
===================

Pixel Physics incorporates the third-party software listed below. Each
component remains under its own licence, reproduced in full here. Nothing in
this file grants any right in Pixel Physics itself, which is proprietary --
see LICENSE.

Generated by `bash scripts/notices.sh` (cargo-about) from Cargo.lock. Do not
edit by hand.

Summary:
{{#each overview}}
  {{count}} component{{#unless (eq count 1)}}s{{/unless}} under {{{name}}}
{{/each}}

The same licence appears more than once below when the components under it
carry different copyright lines. That is deliberate: the notice each licence
requires you to reproduce is the copyright holder's, so the blocks cannot be
collapsed into one.

{{#each licenses}}
--------------------------------------------------------------------------------
{{{name}}} ({{{id}}})
--------------------------------------------------------------------------------

Applies to:
{{#each used_by}}
  * {{{crate.name}}} {{{crate.version}}}{{#if crate.repository}}
    {{{crate.repository}}}{{/if}}
{{/each}}

{{{text}}}

{{/each}}
