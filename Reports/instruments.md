# The instruments — what already exists to measure with

**Status: living index. Current as of 2026-08-26.**

Every measurement in this repo comes out of an `examples/` binary, there are
**49 of them** (`ls examples/*.rs | wc -l`, 2026-08-30 -- the 36 this line
carried was the 2026-08-29 census and had drifted by thirteen, so recount it
rather than incrementing it), and their names do not say what they can answer. This file
exists because instruments were being rebuilt: a lane needs a number, does not
know a harness for it already exists, and writes a second one. The specific
case that prompted this — W3's `divergence` — turned out to answer at least
four questions nobody had asked it, none of them guessable from the name.

**Two rules before you build a new one.** First, grep this page. Second, if you
do build one, add its row here in the same change, and say what it can answer
*beyond* the question you built it for — that sentence is the whole point of
the file.

Two gotchas that apply to every row, both of which have produced whole invalid
studies:

- **`cargo build --release` does not rebuild examples.** Use `--examples`. A
  stale binary runs happily, prints plausible numbers, and has a newer mtime
  than the source you just edited.
- **An unknown argument is silently ignored.** A 3.5-hour study once produced
  eight byte-identical logs because the binary predated the flag it was being
  given. Make a harness echo its own parameters, and treat identical output
  across a change that must have moved something as a stale-binary tell.

## Agents, context and cost

Not an `examples/` binary — these read what an agent run already wrote down.

| instrument | answers | notes |
|---|---|---|
| `scripts/cacheprobe.py` | What the prompt cache actually did in a session: reads, and writes split by **TTL**. Separates the main conversation from sub-agents (`isSidechain`). | Reads `~/.claude/projects/*/<session>.jsonl`, which Claude Code writes for free — **no agent spend**. Beyond the question it was built for it also answers "did this session cache at all", "which TTL is really in use" (measured 2026-08-28: main conversation is **100% `ephemeral_1h`**, 983,153 tokens against 0 at 5m), and "how much did re-reading cost me". Its three questions — namespace, race, TTL — are deliberately reported apart; conflating them is what broke `agent-strategy.md` §6. `--selftest` needs no transcript. Must run on the machine that ran the agents. |
| `scripts/contextbudget.py` | What every session, agent and subagent pays before it starts; `--corpus` ranks the read side. | `--gate` is the ceiling (docscheck check 9b), `--check` the record's staleness (check 9). Token figures are bytes/4.0 and that divisor is **unvalidated** — the gate compares bytes for that reason. |

## Rendering and judging by eye

| instrument | answers | notes |
|---|---|---|
| `filmstrip` | A contact sheet of several frames, or `gif=1` for an animation | The acceptance harness *and* the review-card generator. `channel=` draws per-cell scalars. Reach for `gif=1` whenever the question is whether something *moves* right — a grid of stills cannot answer that, and a GIF has twice got a diagnosis where stills got a rejection. Its per-tile lines carry the **standing organ census** beside the organ event counters, which is the pair a flowering card needs: built says the mechanism fired, standing says there is anything in the picture to see, and a stand once read 1,126 organs built with no fruit on any plant. **For creatures the GIF is not a preference, it is the only instrument**, owner-verified 2026-08-30 on a contact sheet of a starving colony: *"visually, I cannot tell anything from these. ants are mostly visible with there motion"*. An ant is two dark cells at play zoom and is picked out of dark soil by *moving*, so a grid of stills cannot answer any question of the form "is this creature state readable". **And the corollary is a trap worth naming**: a *dead* creature has stopped moving, so it is unfindable by the very channel that makes a live one findable — no camera, crop or sampling rate reaches it, and a corpse-legibility question can only be settled on colour. **`scene=surf` is the only scene in which the gnome swims *along* a surface** — `scene=swim` presses no direction at all, so it dives and hops and has never once crossed a waterline, which made it blind by construction to half of the water-surface work; `surf` also shelves out to a beach, because `swim`'s pool is walled above the waterline on both sides and its gnome has never left the water either (`1 in / 0 out` over 450 frames). Both take `splash=0`, which is `Tuning::splash_force` and the paired control for anything the gnome does to water |
| `ascii` | Headless behaviour plus **worst-frame timing** | The number to quote for frame cost. CI runs it. **`PIXEL_PHYSICS_DROP_MOISTURE=off[:fold]` is the deposition scene's ablation** (2026-09-02): it zeroes `(MoistureGrad, Drop, w)` on the ant genome and folds the weight back into the bias at `fold` of its ceiling, so the arm keeps the drop *rate* and loses only the *dependence*. Two things in it generalise. Zeroing the weight alone is **not** the control -- that scene has no nest, so `AtNest` is 0 and `squash(-0.2 + 0.2)` is exactly 0, and drops go 715 -> **0**: an ablation of a term inside a sum has to hold the sum, or it is measuring whether the verb fires at all. And the patch must run **before** `plant_ant`, because `place_creature` copies the species genome at spawn; written after it, both arms came back byte-identical, which is this repo's standing tell for a change that moved nothing |
| `viewshot` | What the *player's viewport* shows of a world larger than itself | The scale question a full-world render cannot answer. `quarry=WxD` cuts an open-cast pit into the skyline **on the shot after the one it names**, so `shots=2` is the minimum or the cut is never drawn; `aim=` puts it on high ground, without which it fills with sea. **`vault=1` could not find a cave at the shipped 8192x2560 world size until 2026-08-29** -- it searched below `world_h / 2`, which is beneath every cave in a world this deep, and printed `NO VAULT in this world` on seeds whose own pass counter said `systems 1`; it now searches from each column's `surface + vault_min_depth`. With `gnome=1` it also stands him on a **cave floor** rather than the skyline, and prints `NO STANDING ROOM` when no 7x14 box with footing exists in frame -- which is itself the answer to "can the player get in here" |
| `labshot` | **What the evolution lab's box actually looks like, and whether anything is living in it** — builds `lab::scene::LabBox` and renders it through the shipped `Renderer` at chosen frames, as a single-column contact sheet, with the plant census printed under each stop. **`zoom=` was unusable alone until 2026-09-05 and now has the two arguments that make it mean something**: `look=x,y` aims the camera (without it a 4x sheet of this box comes back as eighty rows of empty ceiling, and a 6x one as bare soil — the view does not follow the ground) and `crop=x,y,w,h` cuts the tile, because the box is 512x320 and an ant is two cells, so a full-frame tile is a picture in which the thing being judged is invisible. Its animal line is the pair a creature card needs: how many are **carrying food** and how many **dirt**, the **crop-fill histogram** in quarters, and the **bounding box of every animal**, so a sheet can be aimed at them rather than at a guess. That histogram immediately overturned the cue it was added to check — *carrying food* is not an event, 30 of 35 animals hold a whole cell at any moment and none is under a quarter full, so a binary render rule would paint 85% of the colony one colour | Built because the lab bed is the first scene in this repo to hold **plants and creatures together** — `filmstrip scene=colony` grows its plants from worldgen, `PlantScene` builds beds with no creatures, `creature_probe` builds floors with no plants — so nothing here could look at one. It generalises to **any hand-built scene whose defect would be a scene error rather than a code one**: the counts (`cells`/`orgs`/`seeds`) sit under the picture precisely because a founder that never germinated and one too small to see are the same picture and opposite findings, and `CLAUDE.md` records that mistake costing real time twice. Calls `LabBox::build`, never a private copy, so what it shows is the game's bed and its numbers transfer. **Since 2026-08-30 each stop also names the ants, the births, the standing fruit and every founder's cell count by the id it held before the first tick** -- which is what settles the germination-or-invisibility question the row above states: a founder whose id no longer resolves is dead, one at three cells is merely too small to draw, and the photograph is identical. The founder-id trick works for any organism a scene builder places. **`movelamp=from,to` is the mechanic as one binary and one branch** -- the same bed with a fixture dragged somewhere else, which is what makes an A/B of it a paired comparison rather than two scenes; `lamps=0` is still the control that turns the fixtures off |
| `lamp_probe` | **Whether moving a grow light actually moves the light — one cell at a time — and what that costs**: three modes over `lab::scene::LabBox`. `light` sweeps one fixture's column and reports the bench's light **centroid**, `stand` grows the shipped bed to N frames at each stop with the census beside it, `cost` times two arms off one binary in alternating pairs (`roof` = the lab as it was, sunlit with inert fixtures; `lamps` = as it ships) | **The reusable part is the centroid, and it generalises to any "is this control continuous or does it snap" question about anything living on the coarse field** — pressure, moisture, temperature, a future fan or heater. Position questions get read off a **weighted centroid over the pool** rather than off a peak or a mean: the peak of a block-quantised pool is *constant* while the pool teleports, so a peak reading would have called the broken case clean. It ships with its own **positive control** and that is the point (`LAMP_BLOCK_QUANTISED=1` in `field.rs` puts the block-max emitter back), because a smooth result is exactly what an instrument that cannot see steps also prints — under the control the same sweep sits still for six columns and then jumps 4.0. `mode=cost` carries the trick that made its number readable: **`founders=0 colonies=0` prices the machinery**, because with the bed planted the two arms grow different-sized stands and a stand costs ~0.7 µs per plant cell per tick, so the planted delta is mostly the biosphere the change bought (+0.42 ms) rather than the change (+0.002 ms). Echoes `FIELD_SCALE` in its own header, since every finding it produces is a comparison against it. `Reports/lab-lamps-light-the-bed-2026-08-30.md` |
| `labdial` | **Where a fast-forward stops reading as motion, what the box actually achieves, and a picture of the whole dial** — three modes over `lab::scene::LabBox`: `census` measures how far the things on screen move between displayed frames, `rate` drives the real `Lab` frame loop and reports achieved-against-requested multiplier per display rate, `gif` renders one box at every dial stop side by side as an animation | **The three modes are deliberately three kinds of number, and the split is the reusable part.** `census` is *counters only* — displacements, cells, ticks — so it is valid on a machine with three other agents compiling on it, where `CLAUDE.md` records a byte-identical binary disagreeing 2.42x; `rate` is the only mode that reads a clock and says so in its own header. It generalises to **any question of the form "can the eye follow this"**: the instrument is net displacement of a tracked feature over N ticks against the feature's own size, and it prints its **positive control** beside the answer — at one tick the largest displacement any ant managed was 1.41 cells, one diagonal step and the hard bound, so a 0.00 there would have named a dead probe rather than a null. It also answers **"how much of this box is even moving"**: cells whose material differs between two displayed frames, anchored rather than ringed (a ring of 769 material grids is 250 MB and 2.8 M comparisons a tick, which cost more than the simulation it measured). In the lab bed that number is **under 0.16% of the box even at 256x**, which is the finding that made the ants the whole of the visible motion. `Reports/lanes/evolution-lab-lane-a.md` |
| `uishot` | **What the app's own HUD and panels look like**, and what a held sky or a pinned weather actually does | The only thing here that calls `App::draw` — every other renderer builds frames from `Renderer::draw` and never sees a panel. Drives `App::update`/`App::draw` as `main.rs` does, then sets the state a keypress would have. `sheet=menus\|sky\|weather`; a fresh deterministic world per tile, and per-tile counters (sun, water, gusts and delivered force, freezes, bolts, ice **in the drawn window**) printed under each. That last column exists because FROST measured 3,844 freezes against a picture identical to CLEAR's — the world is 8192x2560 and the frame shows 1/128th of it |
| `scale_covariance` | **Is the same seed at `k` times the cell resolution the same landscape, `k` times as large?** | The question the resolution step's whole content half rests on, and it was worth asking before hand-editing 46 parameters. Reports the rescaled elevation residual against **two controls in the same run** -- an unrelated seed, which is what "no relationship" looks like, and a `region_variation=0` arm, which names the cause when it fails. It did fail: 39.1 rows against a 42.5-row control, diagnosed in one run to `region::COMPOSITION_WINDOW` being a hardcoded 512 |
| `pixel_stat` | How noisy a rendered region is, as a number | Compares two strips without squinting |
| `subpixel` | **What the picture looks like when the renderer has more pixels than the simulation has cells** — the same world drawn at `scale` pixels per cell with each cell's shape reconstructed at sub-cell resolution, against `arm=baseline`, the shipped 1:1 render magnified by the same factor | Not plant-specific and not really a renderer: it is **a testbed for any question of the form "what if a cell were not a square"**, because the colour source is the shipped `Renderer` at 1:1 and the background is a second pass of the same renderer over the same world with the class under test emptied — so no arm can invent a colour the engine would not have drawn, which is what makes an A/B off it admissible. `arm=plants|all` decides whether terrain goes through it too, and that is the comparison worth running before believing any smoothing proposal: with colour blending on, **the soil grain is destroyed**, and the grain is why soil reads as soil. The `ao`/`shade` knobs are the general finding rather than a plant feature — a kernel field over *any* occupancy channel yields a thickness (`cov`) and a surface normal (`grad cov`) at sub-cell resolution, which is volume shading for anything the lattice draws flat. **Set both to 0 first when an interior looks quilted**: two separate lattice-noise bugs came out that way and nothing else caught either (`Reports/subpixel-rendering-2026-08-29.md` §5d). Echoes its own parameters, and prints the plant-cell count beside the image |
| `terrain_shade` | **Does lighting the ground buy anything?** — an A/B testbed for shading terrain over the shipped 8192x2560 world at play scale, with `base|ao|sun|ink|aosun|flat|full` arms drawn through the app's own `Renderer` | **The number it exists for is not the picture: it is the boundary census** — what fraction of the ground on screen lies within one kernel of air, which is the *ceiling on what any shading term can reach* and is a property of the world, not of the renderer. Reads **4.1-4.9%** at the surface on all five presets against **21.8%** underground, which is why the same knob is invisible above ground and transformative below it. That question generalises to any per-cell effect keyed on local geometry — contact shadows, rim light, edge wear, moss on exposed faces — so run it before building one. Occupancy comes from `World`, not from the rendered image (deep air and night rock are both near-black, so an image-derived mask loses exactly the caves); colours come from the shipped `Renderer`, so no arm can show a colour the engine would not have drawn, which is what makes an A/B off it admissible. Prints per-arm "how many subpixels did this actually move", so a null is distinguishable from a disconnected knob, and `base` moving 0 is the null arm's own control. Echoes its own parameters. `Reports/worldgen-visual-interest-2026-08-29.md` |
| `subpixel_cost` | **What drawing the same world region at more pixels per cell costs**, as a paired comparison over one camera | Distinct from `render_cost`'s `viewport_scaling`, which grows the *viewport* at `zoom == 1` and so shows **more world** — its own note says the extra is cheap underground stone. This holds the visible region fixed (asserted, not assumed) and varies only the output lattice, so it answers the resolution question rather than a content question. Reads 1.13x at four times the pixels and 1.32x at nine, because the per-pixel work is under 10% of a full draw and a finer lattice repeats none of the per-draw setup. Generalises to any "is this cost per-pixel or per-draw" question about `Renderer::draw`. Its "under 10% of a full draw" was the fixed cost `frame-cost-the-render-half-2026-08-29.md` later named -- 94% of that draw was one call outside the pixel loop, so read this row's ratios against a redraw whose phase split you have seen |
| `render_cost` | **Where a full-screen redraw spends its time**, broken down, **and what a bigger viewport costs** | The full branch measured 12.07 ms mean on the shipped 2048x640 world -- 54% of a frame -- and runs on ~100% of frames while the gnome walks, because a camera move invalidates every pixel. Its `viewport_scaling` section draws one world at 512x320, 768x480 and 1024x640 -- the resolution question -- and carries **two uniform-world controls beside it**, because the camera is clamped at the world origin and everything a taller viewport adds is cheap underground stone: on the generated world the scaling reads 2.41x at 4x the pixels, and on the all-stone control it reads 3.68x. The control is the number. **Set `PIXEL_PHYSICS_DRAW_TIMING=1` on this or any harness that draws** and `Renderer::draw` prints its own phase split -- preamble, horizon, sky light, glow splat, pixels, overlays. Nothing else in this table can attribute a redraw *internally*, and this row's own breakdown is reads-vs-colour-work rather than phases: it read 3% for the per-pixel chunk lookup while **94% of the frame sat in one uncounted call** (`Reports/frame-cost-the-render-half-2026-08-29.md`) |
| `frame_profile` | **Which phase a frame's time went to**, timed separately with a distribution | The thing `ascii` cannot answer: it reports a worst frame, which says "does this fit in 16.6 ms" and nothing about *where* it went. Runs the exact phase list `App::update` runs |
| `camera_snap` | Whether the camera moves discontinuously through the path the **app** actually uses | Drives `App::update`/`App::draw` as `main.rs` does, rather than calling `Renderer::follow` directly -- so it catches what a harness calling the API itself cannot |
| `weather_duty` | How often it is raining, swept across seeds and a long window | Built because a single 1,200-frame run measured 89% and that was a sample from inside one wet epoch, not a duty cycle. Generalises to any "is this a duty cycle or one epoch" question |

## The evolution lab

| instrument | answers | notes |
|---|---|---|
| `lab_cost` | **Does the lab box live, and what does it cost?** — the plant and creature census over a long run, the organism-slot ceiling, the Gate 0 birth arithmetic in *this* bed, and whole-frame cost as the stand grows, with the achievable simulated-seconds-per-real-second at 60 Hz and 20 Hz | Gate 1 and Gate 3 of the evolution-lab design guide in one command. Three things it answers past its own question: `selftest=1` is a **positive-control suite over its own counters** (does the split tick reproduce `frame::step`, does a fan actually wake the box, does the leaf census read zero in an unplanted bed) and is the model for any harness that must not publish a null it cannot have moved; `walls=1,2,4,8,16 reps=N` runs **paired, round-robin** arms rather than arm-after-arm, which is the only timing shape that survives a loud machine; and `gut=` overrides the ant's diet gene *before the colony is founded*, so it can construct the case whose answer is known non-zero rather than only measuring the shipped one |

## Plants

| instrument | answers | notes |
|---|---|---|
| `divergence` | **Does one environmental difference produce two different-shaped plants?** | See below — the most reusable thing here |
| `fate_viability` | **What fraction of mutations to a species' production rule still produce a plant that lives and breeds?** | Gate 1 of the evolvability programme, and it is allowed to answer *no*. Generalises past plants and past fates: it is really *"register N variants of a species at runtime, grow each, and classify the outcome"*, so any question of the form **how much does this substrate tolerate being changed** can be asked by swapping what `mutate` perturbs — a creature genome, a material table. Its real contribution is the **three-way** classification: viable / lethal / **silent**, where silent means the stand came out identical to the base and the mutated field is therefore never read in that scene. A two-way rate counts silent mutations as tolerance and overstates robustness by however many of them there are (measured: 7 of 48 were no-ops from drawing the value already held, and a further set were unread fields). Both controls are mandatory and print on every run — the unmutated table must live, and a shoot-child-to-`Seed` table must die — because a viability rate with neither is exactly the arithmetically-correct-and-about-the-wrong-thing shape. The positive control has already earned its keep once: it reported the *unmutated* table as 0/3 and caught a harness bug that would have published a decisive, entirely false 0%. **It calls the shipped operator rather than imitating it**, which it did not until 2026-08-29: it mutated its own copy of the table with its own code, drawing from six cell types on the woody base where `FateGenome`'s operators draw from eight on every base — so its number was about a mutation nothing in the engine performs. Route is now genome -> `FateGenome::mutate` -> `to_table` -> RON -> founder genome. **`op=all` (default) or `op=retarget|recondition|insert|delete`**: `all` reproduces the shipped 60/15/15/10 mixture, and forcing one operator is what makes the rare ones measurable — 40 mutants of the mixture spend about four draws on `delete`. **Four outcome classes, not two**, and the two extra are where a naive rate leaks: *declined* means the operator itself changed nothing (a redraw that never found a different value, `delete` at the one-rule floor, `insert` at `MAX_FATES`), *silent* means the genome changed and the stand came out identical anyway. Declined is a subset of silent; both are excluded from the denominator, because a mutant that establishes only because it *is* the base is the positive control quoted back as a result. **`base=tree` (default) or `base=herb`**: `tree.ron` declares no organ material, no `Ripen` behaviour and no `Ripe` rule, so a mutant reaching `Flower`/`Fruit` there measures that gap as well as the substrate — the harness no longer prevents it (that was the divergence above) but counts it in its own column. Note the negative control finds its rule by `when == Grew` rather than by index, because a determinate base carries an extra rule ahead of it and a control that poisons the wrong rule fails *open* |
| `plant_probe` | Every organism-owned cell's per-cell channels for a grown tree | The quantitative pair for any `channel=` overlay. Echoes its own parameters. Also carries the four **organ** effect counters (built / axes terminated / fruit dropped, plus three separate *binds* ratios), which appear only for a species that has organs **`GENERATION CLOCK` is the cumulative readout and it answers what no other generation number here can.** Every other one in this repo is a max or a mean over the *living*, and those equilibrate once births balance deaths rather than accumulating -- `selection_arena` records a mean rising to 2.9 over 150,000 frames and then drifting back to 2.60, and prints its own axis as saturated. `World::deepest_generation` is a high-water mark and only goes up. **It caught its own artifact on the first run**: `tree` reads deepest-living 1 against deepest-ever 2, `scrambler` 4 against 5. Measured levers, herb, 8 founders, 30,000 frames: species is 2.5x (tree 15,000 frames/generation against herb and scrambler at 6,000) and `maturity=` 60->20 a further 1.6x, together **4x**; `hazard=0.02` is a measured **null** on depth and costs 11% of the births. |
| `genome_drift` | **Per-slot population mean over generations** — whether a slot ever moves — **and, since 2026-08-29, whether the *production rule* moves** | Warns below generation 2, because a drift study on a population that never turns over cannot answer its question. The continuous slots and the fate table need different treatments and get them: a population mean is meaningful for a draw and meaningless for a *program*, so the rule table is censused instead as *how many individuals carry a table that is no longer their species'*, split by whether it grew (`insert`), shrank (`delete`) or changed in place (`retarget`/`recondition`), plus the count of distinct tables alive. **Two free controls, and read them first**: `gen0` must be 0, because founders take the species table verbatim, so anything else means the census is reading the wrong object and every other column is void; and `empty` must be 0, because an empty genome falls back to the species table and a population of them reports *no drift* while meaning *no genomes* — the channel-with-no-writer failure wearing a plausible number |
| `genome_reach` | **What phenotypes can this species' genome actually reach?** — the reachability census, in two halves: a static table of what the arithmetic permits, and a `grow=1` arm that widens one slot to its maximum and hashes the whole grid | The complement to `genome_drift`, which answers *does a slot move* about the **draw** — and every slot's draw moves, always, because `seed_genotype` fills the vector unconditionally whether or not anything downstream reads it. What decides whether a lineage can express a different plant is the map from draw to phenotype, `base * (1 + draw*variance)`, and **both of its other arguments are authored per species and never inherited**, so the reachable set is a closed interval fixed when the `.ron` is written. Two ways it collapses: `variance == 0` (usually just slot ownership — slots 1/5/8 read the `RootTip` vector) and `base == 0`, which is a cage no mutation can leave. **Three things in it generalise past plants.** The dynamic arm is a **whole-world hash, not a phenotype summary**, and the asymmetry is the point: outcomes here are chaotic in the seed, so a *changed* hash is weak evidence of importance and an *unchanged* one is conclusive evidence of nothing — any question of the form *can this channel move anything at all* wants this shape rather than a metric. Its positive control (a slot the static table calls live must move the world) is asserted, because a harness that patched the wrong species or patched after the founders were planted reports every slot dead and looks decisive; its negative control is slot 9, which has no consumer. And **a null in it at a short horizon is a statement about the scene**: at 4,000 frames five of eight live slots read `identical` and at 16,000 all eight moved, because the stand had not yet thickened or rooted to depth. **It corrected its own static half**, which is why `bases_of` returns a list: slot 1 has **two** consumers — it multiplies the root's `branch_chance` *and* divides its `branch_priming` — so a census that read one call site reported `tree`/`conifer`/`shrub` unable to evolve a branching root system, and the widening arm disproved it. A reachability census taken by reading one call site is a census of that call site. `drift=1` is the third mode and it can only be run against `organism::ParamGenome`: it grows a stand at a chosen `rate=` and censuses the standing population's override tables, reporting **how many sit at their `clamp_param` bound**, which is the free-lever signature `plant-heritability-survey-design-2026-08-27.md` §2 predicts and §4a only inventories. It refuses at rate 0, because an empty table reads exactly like *nothing drifts*. **`rhizome=1` is the fourth mode and it produced the finding this harness was not built for**: it registers a runtime variant of `herb` (root `plastochron` plus a `RootTip` fate whose `lateral` is a `GrowingTip`) beside the shipped species, touching no file, and asks whether clonal spread is reachable. **Two censuses failed before a counter worked and both failures generalise**: shoot tissue below the ground line reads 1/3/2 in the *unmodified* species (a collar buried by a cell of moving soil looks exactly like a shoot from below), and restricting to four rows down does not fix it either -- the control reaches **nine** rows on one seed. `World::root_shoots_launched` is the counter, and it reads control **0 / 4 / 0** against treated **15 / 13 / 29**. The middle row is the result: the control is not zero, and `PIXEL_PHYSICS_FATE_MUTATION_CHANCE=0` takes it to 0/0/0 -- so **a lineage already discovers a shoot growing off a root in the shipped game**, by `Retarget` pointing the root's `Grew` rule's `lateral` at a `GrowingTip`, with no `Node` fate needed at all. **And then the follow-up, which is the half that answers the design question**: `emergent_clumps` counts each organism's 8-connected above-ground clumps and calls one a *stem* only if it is 8-adjacent to a cell of the same organism at or below the ground line -- one plant, one collar, is one stem however deeply that collar is buried, which is what makes it immune to the burial trap that killed the two depth censuses. Over eight seeds: **144 launches produce 5 second stems against the shipped species' 3**, and all three of *those* occur on seeds whose launch counter reads **zero**, so the background is not suckers at all -- a plant's base split by a grain of surface soil. `above_ground_width` closes the one loophole (a sucker surfacing *inside* its parent's crown merges into that clump and cannot raise the count) and the treated arm is **narrower**, 12.04 against 12.84, so nothing surfaced anywhere. The stand pays 22% of its plants and 30% of its cells for the attempt. **Its positive control is the part to steal**: it grafts a column of the plant's own shoot tissue out of one of the plant's own below-ground cells -- a sucker built by hand -- and asserts the stem count moves by exactly one, on all 16 runs. Two earlier versions of that control failed and both failures generalise: an *unanchored* stamp tested only what the census is designed to reject, and a control that **hunts** for a clear site found none on the densest bed of nine and printed `None`, which reads exactly like a census with nothing to find -- so it now clears its own site and re-takes its baseline afterwards, and whatever the clear removed cancels. `Reports/plant-engine-rethink-2026-09-03.md` |
| `clone_variance` | **How much of the difference between two plants is their genome, and how much is where they happened to stand?** — broad-sense heritability per shape descriptor, `H2 = 1 - Var(clone arm)/Var(population arm)` | Built for the owner's *"clones of the same plant end up growing/looking very different from one another, which makes it much harder to identify when growth patterns do change"* — a scatter this repo has recorded since 2026-08-27 as a **method note** (*do not quote a stand median*) and never as a defect. The consequence it measures is the one nobody had drawn: if developmental scatter is larger than genetic difference, **selection cannot see the genome either**, and the population re-rolls its variety every generation instead of inheriting it. Measured on `herb`: **H2 on plant size is 0.013 / 0.054 / 0.000** over three reference genomes x four world seeds, against a positive control reaching 0.75–0.82 — so size is the least heritable thing the engine produces within a species, and composition is the heritable half. **Four designs in it transfer to any variance-decomposition question.** The `spread` arm is mandatory and is the estimator's sensitivity control: half the founders at every continuous draw `-1` with allele 0 at every locus, half at `+1` with the top allele, i.e. the widest contrast the engine can express — if no descriptor separates *that* from a clone stand, the descriptors are blind and every H2 above them is void. Variances are pooled **within** each seed and then averaged, never pooled across seeds, or the between-world difference sits in both arms and drags every ratio toward 1. `ref=` sweeps which founder is cloned, because one genome is one sample and a founder sitting near a threshold in the economy makes every clone of it sit there too. And `shift=1` is the arm that names the mechanism rather than sizing it: **one founder, alone, in an identical bed, moved one column at a time**, which isolates the fact that `plant.rs`'s growth draws come from `rng::stream(organism_id, cx, cy, frame)` — so a plant one column over is a different plant, and a plant that germinated into organism slot 7 rather than 6 is a different plant again, which is neither environmental nor heritable. **Its own first version was vacuous and the tell was the standing one**: `ref=0`, `ref=1` and `ref=5` produced byte-identical output, because `PlantScene::build` plants through `World::plant_tree_species`, which never calls `plant::seed_genotype` — so at frame 0 every founder holds the species mean and they are already clones of each other. Needs `World::set_organism_genotype`, which also sets `inherited` or germination redraws over the genome it was just handed. **`spread=` is a fourth mode and has nothing to do with those three arms** -- `spread=0,1` renders one bed per `Behavior::Grow::leaf_spread` setting and prints `leaf_shape_census`: leaf cells, clusters of three or more, and mean elongation, the long side of each 8-connected cluster's bounding box over its cell count, so a line reads high and a blob low. The word collides with `Arm::Spread` and means something else; read the argument, not the name. **The leaf-cell count is the control and not the answer** -- if it moves, the two beds differ in how much foliage there is and the elongation reading is about quantity rather than shape. Measured on `herb`, 10 founders, 14,000 frames: elongation 0.634 -> 0.769 across `leaf_spread` 0 -> 1 while leaf cells went 3,203 -> 3,389, i.e. 21% of shape on 6% of quantity. It sets the value through `SpeciesRegistry::set_param` on the live registry rather than editing the `.ron`, because a prebuilt binary reads the species through `include_str!` and an edited file produces bit-identical runs. **Its spread arm was developmentally uniform for a while and its own control said so, which is the part to steal.** Under `DevelopmentalKey::Plant` the `H2 (control)` row read **0.000 on all seven descriptors** against 0.44-0.82 under the shipped key -- the estimator declaring its own numbers void. A founder has no lineage seed until something draws one (`PlantScene::build` never calls `seed_genotype`), so the arm read 0 off every ungerminated founder and wrote it onto all sixteen, collapsing `Var(spread)`, the denominator of every H2 above it. **Before reading any `H2` row here, check the `H2 (control)` row beside it is non-zero** -- that is the only thing that catches this class, and it caught it where a byte-identical output (the `ref=` failure, same file) would not have. `dev=` is the fifth mode: `world` is the shipped key, an integer is `Plant { coarseness }`. `shift=1` under `dev=0` is the acceptance test for the developmental seed and reads CV **0.280 -> 0.074** on `cells`. `Reports/plant-engine-rethink-2026-09-03.md` |
| `clone_identity` | **Can two clones grow *identically*?** — the same question `clone_variance` answers as a variance, asked as a yes/no. Reports a **divergence frame** (the first moment two clones differ by one cell) and a translation-invariant Jaccard over each plant's cells expressed relative to its own collar | Built when the owner's answer to two rounds of `clone_variance` was that a clone bed still shows *"an extreme amount of variability"*. **A CV cannot say whether the residue is one cell or a whole crown, and a final distance cannot say whether they were never alike or alike for 20,000 frames and then split** — only the onset separates those, and they want different fixes. **The answer, measured 2026-09-04:** two clones in the same column are cell-for-cell identical (Jaccard **1.0000**, both arms of the control); one column apart they are identical for thousands of frames and then diverge, ending at 464 against **1,057** cells. Divergence does not decay with distance — 1 column apart reads 0.29 shared and 192 columns apart reads **0.98** — which rules out a spatial gradient and names chaotic sensitivity instead. **Its ablations are the transferable part**, each removing one candidate: `solo=1` gives each clone its own world (neighbours gone), `clear=1` pins `Pin::Clear` (no gust, no rain — divergence landed on the *identical frame*, so weather is not the trigger), `sterile=1` stops the bed breeding, `dev=` selects the developmental key, and `columns=a,b,c` plants one tree per world at each column so that **column and organism id do not move together** — `solo` changes both, and the id is not inert (`(frame + id) % ORGANISM_TICK_INTERVAL` is a plant's tick offset). **Run `columns=256,256` first**: two arms at one column must read 1.0000, and without that control every number here is noise. **The distribution is the finding, not the spread**, and a dense sweep is what shows it: 33 consecutive columns, one genome, 6,000 frames gave a median of 198 cells with the middle half inside **190-200** and **28 of 33** within +/-15% of the median, against outliers at 225, 242, 243, 338 and **487**. CV over all 33 is **0.286**; dropping the three largest leaves **0.074**. So four-fifths of clones are nearly the same plant and a minority escape -- which is a different defect from "development is noisy" and a far more tractable one. There is **no period at 4, 16 (`FIELD_SCALE`) or 64 (`CHUNK_SIZE`)** across those columns, so it is not a grid alias either; the bed is uniform to the byte, the only per-column variation being the soil cell's shade, which `frame.rs` hashes and nothing else reads. `Reports/plant-engine-rethink-2026-09-03.md` |
| `selection_arena` | **Does this world punish a plant that is worse?** Two genomes compete in **one** bed for the same light, water and space; the readout is which arm owns the world at the end | The teeth-test, and it is aimed at the **environment** rather than the genome — closing the gap `plant-fate-operator-gate-2026-08-29.md` §6 names in as many words ("the gate asks whether a mutant lives, never whether it wins... that needs competing arms in one world, read at an order statistic"). A null here is a finding about the *world*: it says the bed does not discriminate against a plant known to be worse, so no evolution result measured in it can be trusted. **`arm=` is a ladder, not a switch** — `same` (control), `lethal` (the shoot's `Grew` child becomes `Seed`, `fate_viability`'s own mandatory negative control, found by `when == Grew` and never by index because a determinate base carries an extra rule ahead of it), `nobranch`, `norootbranch`, `early` — so it reports *where* the world stops discriminating rather than yes/no. **Attribution is by `OrganismState::lineage`, never by genome**, which is what makes the identical-arms control measurable at all. **Read the seed count, never the median**: measured on the unmirrored control with two copies of *one* genome, arm B's share ranged **40.9%–80.5%** across seeds from position and genotype draw alone, and the median read 63.7% with no true effect — while the direction statistic sat correctly at the null. **`mirror=off` is not an option, it is the control**: mirrored, `arm=same` is one simulation with the labels swapped, so `A == B` is an algebraic identity and its exact 50.0% says nothing (that was the first result this harness produced, and it is vacuous). Refuses to run if the `arm=` edit matched no rule, because two identical arms read as a clean 50/50 — indistinguishable from the finding it exists to make. **`sky=clear` is what makes a bed dry, and `moisture=` alone is not**: measured 2026-08-30, a bed under live weather is rained on for **30% of a 20,000-frame run** and carries 74,674 units of ponded free water that roots drink directly, while nothing dries a synthetic bed at all (`evaporation::schedule_damp_soil` fires where soil is *wetted*, never from the sweep). `sky=live` is the default and is byte-identical to the argument not existing. **`bed=1` censuses the bed instead of racing in it** -- available soil water in the rooting zone, ponded free water, and every established plant's root cells, contact roots, spread, depth and `water_status` -- with `arm=` applied to *every* founder, so `bed=1 arm=X` against `bed=1 arm=same` is a phenotype A/B rather than a competition. Run it before believing any arm measured in a bed whose resources were changed: `Reports/plant-water-scarcity-2026-08-30.md` |
| `root_contact` | How much of a root system is actually touching soil | |
| `plant_reach` | **How far does the vein model actually carry carbon through a grown tree?** Charges the deepest rows of a root system to `RESOURCE_SCALE`, zeroes every other cell, then runs `organism::transport` and **nothing else** | Built because the conductance model's reach can never be read off a running stand: `allocate_to_frontier` pools every non-frontier cell and writes each tip its share directly, so the tip's carbon is *assigned* and whatever transport delivered is superseded. Switching the pool off is not the way to see past it -- that starves every root tip, the documented failure the pool was built to fix -- so this runs the transport pass alone, where anything appearing higher up **must** have arrived through the conductance model. **Two fronts, not one**: a *trace* front (any carbon) says how far the numerics reach, a *usable* front (a tenth of the scale, half of `tree.ron`'s `cost`) says how far a tip could actually be funded, and reporting only the first would call a front that cannot pay for anything a delivery. **Two arms, and the finite one alone is arguable** -- a fixed charge necessarily stalls once it has spread, so `hold` refills the band every tick the way a root system that keeps drinking does, and **the held arm is the one to quote**. Measured seed 1, a 6,257-cell tree spanning 145 rows: finite, the usable front advances 20 of the 141 rows in 1,800 frames and stops; **held, with total carbon climbing 304 -> 971 the whole time, it advances 21 and is flat for the last ten stops**. That is a steady state, not a source running dry, which is the distinction the second arm exists to make and the first arm cannot. **Its total-carbon column is the instrument's own control** and reads the right way round -- `transport` clamps at `RESOURCE_SCALE`, so inflow into an already-full charged cell is clipped and the total *falls*; a total that rose would be a probe bug. `Reports/plant-roots-and-transport-2026-09-05.md` |
| `plant_severance` | **Does the plant economy notice its own plumbing?** `control` / `sever` (the plant's own cells cut from three rows above the soil line) / `deroot` (every root cell removed), each with a `_noload` twin that sets `World::plant_load_failure` false at the cut | **`unreached` is the positive control and the row to read first**: `anchor_support` writes `u16::MAX` into any cell with no path to an anchor, so a sever that leaves it at zero did not sever anything and every number beside it describes an intact plant. **It refuted the hypothesis it was built on, which is the result worth having**: at the shipped default a clean basal cut is correctly *fatal* -- the crown leaves the organism inside one stop via `rigid::fell_severed_tissue`, and the rootstock is rotted 221 -> 21 cells -- so the economy's attachment-blindness is invisible until `plant_load_failure` is off, which is the `_noload` arms and is how the owner plays the lab box. Three things in it generalise. It cuts **the plant's own cells**, never a radius of world, because an axe bite also throws soil and rock and would put a structural disturbance in the arm beside the treatment. It tracks the **largest `track=` plants at the cut**, not the bed: a stand recruits, so a median over every organism is a median over seedlings and the founders' numbers were invisible behind a stand median flat at 106. And `fine=`/`finefor=` exist because **the coarse schedule was wider than the whole window the harness was built to see** -- the crown was gone between one 4,000-frame stop and the next. **Its first version printed a ratio of medians and that is the correction to carry**: `median(water)` beside `median(capacity)` describes no plant that exists -- seed 1 at frame 32,000 printed 202.6/1088 (0.19) while the plant carrying every other number in the row sat at 969.7/1088 (0.89), which is the second occurrence of a mistake `plant.rs`'s own stomatal census already records. It now divides inside the plant and medians the fractions (`fill`). The second column added at the same time is `worst`, the **minimum** status beside the median, because `settle_water` clips status at 1.0 and an upper median over four plants reads 1.000 whenever any two are saturated -- masking a largest plant at 0.677. **A ceiling-clipped channel needs its low tail printed or the run reports a dead coupling where there is a quiet one**: across six seeds `water_status` at the cut reads 1.000 / 0.959 / 1.000 / **0.582** / 0.872 / 1.000, so the coupling is quiet, not dead, and a single-seed reading of it was what `plant-roots-and-transport-2026-09-05.md` §3z had to withdraw. `Reports/plant-roots-and-transport-2026-09-05.md` |
| `flora_census` | Which species a generated world actually contains, per seed | `where=1 focus=NAME at=X` audits one *window*. Built after a card came back "I don't see a difference" and the window held 125 grass cells against 7,853 woody |
| `litter_probe` | Where shed litter comes to rest, and whether it rots. **Read the `SUSPENDED (air underneath)` line, not the on-terrain/against-plant split** | `out=` writes a classification overlay — magenta on a branch, cyan on the ground — and **`crop=x,y,w,h zoom=N` on top of it, which is what makes that overlay judgeable**: the answer is read by eye and a 512x320 sheet with the interesting part 180 px wide is not. `plain=1` drops the markers and the dimming and draws the scene in its own colours -- the marked overlay says which cells are held off the ground, `plain=1` says whether a person would call it a forest floor, and those are different questions. **Two of its columns are confounded and the third is not**: `against-plant` scores a drift banked against a trunk and a leaf stuck up a tree identically, and the height bands measure against a `terrain_top` that excludes litter, so the top of a deep mat reads as high up. `SUSPENDED (air underneath)` is the unconfounded one and is what to quote |
| `crown_census` | **What the brown cells in a crown actually are** — every material standing above the ground line, split into 40-row bands | Built because soil, litter, deadwood and thickened wood are one mid-brown speckle at contact-sheet zoom, so the eye cannot separate them and only a count can. Generalises well past plants: it is a material-by-height histogram, so it answers *what is stacked where* for any vertical structure — a collapse's debris column, a drift's profile. Echoes its own parameters |
| `beam_probe` | **Would a bending-stress model say anything useful about this plant?** Dumps per-cell moment, section and stress for the largest organism, with a parent forest rooted at the structural anchors | Written as a positive control *before* the mechanism, which is the whole reason it exists — it answered "yes, and the base of a balanced stem reads ~0", which redirected the plan (`tree-mechanics-plan-2026-08-29.md` §4). **Reports three section measures side by side and they disagree on 52-61% of cells**, which is the thing to know before quoting any number off it; §9 of that report says why. Echoes its own parameters |
| `debug_tree_variants` | Multi-variant comparison of `tree.ron`'s economy | **Parse fixed 2026-08-27, scene still dead — do not reach for this.** It emitted `moisture_threshold`, renamed to `soil_water_threshold` in `organism.rs` some time ago, so it panicked on start for anyone who tried; the rename is corrected but its scene is a bare stone floor that can germinate nothing, so it answers no question today. `plant_probe` took over ensembles. Kept, not deleted, because its header records why the shipped tree stalled at ~10 wood cells. Its row said only "marked throwaway in its own header" for the whole time it was broken, which is why check 5b now exists |

## World, terrain and weather

| instrument | answers | notes |
|---|---|---|
| `cave_probe` | What a world's caves actually *are*, over a seed sweep | The shape numbers the beauty argument is about, read back off the finished world rather than counted at write time. **Its `formation base width` is the widest single *row*, and that cannot tell a cone from a wire with a lump on the end** -- read `formation mean width` (body cells / height) and `hairlines over 3 gnome-heights` beside it, both added 2026-08-29 when the pass was reporting `base-width med 19` for formations drawn one cell wide. The reachability half (`reachable`, `largest walkable`, `walkable regions`) opens the void by the real 7x14 player box, so it answers *"could I even enter it"* rather than *"is it big"* -- and generalises to any "does the character fit through what I generated" question. `Reports/cave-redesign-2026-08-29.md` |
| `wind_probe` | What `weather::exposure` reads across a landscape -- and, with `velocity=N`, **whether the wind this engine already has reaches the cells a seed falls through** | The `velocity=` arm is the prerequisite for a dispersal *verb* (`Reports/plant-engine-rethink-2026-09-03.md` §7 item 6), asked before building one -- `CLAUDE.md`'s *check that a planned step can demonstrate itself, before promising it will*, after a whole phase of plant work went on levers that fired perfectly and moved nothing. It censuses `field_at(x,y).vx` over the rows above each column's own surface, so the terrain decides where the band is rather than a fixed row. **Read the share over the threshold, not the mean**: the question is what fraction of a seed's flight is windy enough to steer it, and the threshold quoted is the consumer's own (`update.rs`'s `WIND_BIAS_THRESHOLD`, 0.01) rather than a new one. Measured on `rolling`, seeds 1/3/7 at 4,000 frames: **35.7 / 47.9 / 49.3%** of those cells are over it, max |vx| **1.70 / 1.76 / 2.87**, and **nothing reaches `WIND_BIAS_FULL_SPEED`** -- so the wind is real down there but never at full strength, and it is *concentrated*: the median is ~0.005 while p99 is 1.5-2.8. A channel that is near zero almost everywhere and strong in a few places disperses seeds in a few places, which is what dispersal wants. **The `Pin::Clear` arm is the control and it reads exactly 0.0000 on every seed** -- necessary because `field::SETTLE_EPSILON_VELOCITY` means still air settles *near* rather than at zero, so without a calm arm the harness cannot tell a breeze from the solver's own residue. `Reports/plant-engine-rethink-2026-09-03.md` |
| `sky_light_probe` | What a sky-visibility model would say, on the five geometries that decide it | |
| `underground_probe` | How much open air the renderer draws as cave interior | |
| `scale_probe` | What a bigger world costs, measured rather than extrapolated | **`phases=1` is the whole-frame one** — see below. Each row also prints **`chunks` and `solved/frame`**, which is what tells "the field got cheaper because the world is smaller" from "the field had less to solve because the world generated differently" — every size regenerates a *different* world, not a scaled one, and on 2026-08-30 that distinction reversed a conclusion: 8192x1024 has 60% fewer chunks than the shipped size, solves 32% *more* tiles, and costs **more** per frame |
| `pass_ablation` | Which generation pass eats which other pass's output | **`gate=1` turns it into a check that fails**, which is the half it was missing: it found `brows` deleting 100% of boulders on 2026-08-20, and because nothing *ran* it the feature was still absent nine days later. The gate asserts only the two things a pass counter cannot say -- no pass "APPEARS" when another is switched off (the interference signature), and every pass writes somewhere -- and deliberately does not bar a suppression's magnitude, several of which are the generator working. `scripts/worldgencheck.sh` wraps it and carries a `--selftest` that puts R4-1 back (`PIXEL_PHYSICS_BROW_YIELD=0`) and requires the check to go red. **The ablate-and-difference-the-whole-report design generalises** to any pipeline of stages that share a mutable output, which is what `ant_ablation` is independently. |
| `world_look` | **How much of the picture is X?** — for a generation pass, a preset, a material or a render setting. Composition and colour censuses over a *player viewport*, per-pass ablation measured in **rendered pixels**, and a between-group/within-group picture distance | The unit is the point: a world is 8192x2560 and a viewport is 1/128th of it, so `pass_ablation`'s cell counts and this file's pixel counts disagree by **two orders of magnitude** — `moisture_init` writes 298,239 cells and moves **0** pixels, `vaults` writes 43,208 and moves **0**. Both numbers are right; only one reaches the owner. **Two designs generalise well past worldgen.** *Ablate-and-diff-in-pixels* is paired by construction — cameras are computed from the un-ablated arm and reused, so a change that lowers the terrain cannot be scored for panning the camera — and works for any switchable mechanism that draws. *Between-group against within-group distance* asks **is this category real, or is it inside the noise of that one?**, which is the question `plant-appearance-design.md` and `creature-appearance-design.md` both needed and neither had; it found `rolling` and `terraced` closer to each other (0.149) than `rolling` is to itself at another seed (0.173). Renders off the shipped `Renderer` with **daylight pinned** (`creature_look`'s rule), bins colour at the width of `render.rs`'s own grain jitter — an unbinned count reads 25x too many colours and would say the palette is rich — and carries **`mode=control`, six checks that bracket every metric at both ends**, including a one-cell lava poke that proves the pixel-to-cell mask is aligned. Echoes its own parameters. **`mode=strip` is the worldgen review sheet** and the answer to a trap rather than a convenience: `filmstrip`'s `scene=worldgen` calls itself *"the thing worldgen is judged on"* and builds a **512x320** world where `boulders`, `vaults` and `springs` cannot fire at all, so six rounds were judged on pictures of a place the player never sees. `mode=strip` lays `views` player viewports of the **shipped 8192x2560** world out as one contact sheet -- a filmstrip with place along the axis instead of time, which is what a world that arrives at rest wants -- and prints the pass table under it with any pass that wrote nothing named first. `Reports/worldgen-appearance-audit-2026-08-29.md`, `Reports/worldgen-drains-2026-08-29.md` |
| `field_cost` | What the coarse field costs per frame, and what decides it | |
| `labbox_cost` | **What a sealed box of soil, plants and creatures costs per tick, and which world system is paying for it** — four arms over one hand-built bed (`live` / `calm`, weather pinned / `lab`, plus a held grow light / `floor`, the field not called at all) | Built for `evolution-lab-feasibility-2026-08-30.md` and generalises to **any question of the form "what does this world's *background* cost, as opposed to the thing living in it"**. It differs from `scale_probe phases=1` in the one way that decided that report: the bed is `PlantScene`, so there is no worldgen, no terrain variance and no seed sweep to divide out — the same box every run, which is what makes a one-switch A/B admissible. **Every timing carries the stand census beside it** (`cells`, `orgs`, `seeds`) and that is not decoration: the `floor` arm reads 0.007 ms and **14 cells / 0 seeds**, because light is delivered through the field, so it measures a dead lab rather than a cheap one. Read `solved/frame` and `awake/frame` for the same reason. Its own findings worth knowing before reaching for it: an **empty** box costs 0.001 ms (the sleeping machinery is perfect, so all cost is bought by life, at ~0.7 µs per plant cell per tick); **width itself is free, and what it costs is what grows in it** — re-measured on a quiet box 2026-09-01, three legs, median of three alternating rounds at `RAYON_NUM_THREADS=4`. An **empty** box (`trees=0`) costs 0.001 ms at 256, 512 *and* 1024 alike, `solved/f` 0.0 — **1.00x over a 4x width range**, so the sleeping machinery scales perfectly. At **fixed founders** the frame goes 3.575 -> 5.365 -> 6.444 ms, i.e. **1.50x then 1.20x — more expensive, not cheaper**, which is what this row claimed until now. The claim was reading a confound rather than a width effect: `PlantScene` spaces founders `width / (trees + 1)` apart (15 cells at 256, 60 at 1024), so a narrow box is a **crowded** box and a wide one grows 1.94x more plant off the same 16 founders (5,411 -> 8,861 -> 10,483 cells). Normalised, the cost is flat: **0.661 / 0.605 / 0.615 ms per 1,000 plant cells**. `LabBox::spread` divides its span the same way, so the confound belongs to the phrase *"at fixed founders"* and not to one harness. The positive control that licenses those nulls is soil 40 -> 240 at one width, which moved **1.43x** against a run-to-run spread of 1.6-5%; and soil depth costs 1.9x from 40 to 240 rows for a **byte-identical stand**. `arms=` selects a subset. Echoes its own parameters, and finds noon by maximising `sky_light_amplitude` rather than assuming a phase — assuming `period/4` pinned a dim sky and reversed the sign of the grow-light result. **`fans=N walls=N` is the equipment half** -- `add_pressure_impulse` sources and floor-to-ceiling stone partitions -- and it answers *"is a fan just weather?"* (yes: one fan wakes every tile in an open box and costs what outdoor weather costs) and *"does a wall contain it?"* (yes, and gradedly: 16 compartments take a fanned 2048-wide bed 4.097 -> 2.207 ms). Offsets each fan a third of a spacing so it never lands on a partition -- it did in the first sweep, and containment measured as zero. **Pair it with `FIELD_MOMENTUM=0`** (`field.rs`, a control and never a setting) for the question it was extended to answer: *does the air's own motion change what grows?* — measured at 11–20% of the frame for a stand that comes out 1–12% **larger** without it, in 3 of 3 seeds |
| `burrow_probe` | **Does a dug void survive in this material?** A shaft, a gallery and a chamber are carved into a hand-built bed and each is censused separately as it fills in, over `soil` / `sand` / `stone` arms | Built to settle whether ants can tunnel (`Reports/evolution-lab-gui-physics-2026-08-30.md`); the answer is a gallery gone in **5 frames** and a chamber in **30**. **Two things in it generalise past digging.** The three voids are censused apart rather than pooled because they fail for *different* reasons — a shaft is a vertical face and tests the repose rule, a gallery has a roof and tests the straight-down rule, a chamber is both over a longer span — so the shape is the template for any question of the form *"which of several mechanisms is closing this"*. And its **frame-0 assertion is the scene check made mechanical**: every carved cell must read open before a tick runs or it panics naming the geometry, because the first version of this file reported **0% galleries at frame 0** — `soil=140` under `ground_y=200` puts `PlantScene`'s bed past the world's bottom edge, where `World::set` silently drops it, and that reads as an instant, total collapse. Any harness that carves, paints or stamps a scene wants the same assertion. The `stone` arm is the positive control and must read 100% in all three voids at every frame; a run where it does not is measuring its own scene. Censuses on raw `material == EMPTY`, never `Cell::is_empty`, which is managed-aware and answers a different question. **`arms=colony` is the arm that is not a hand-carved cavity** -- 55 ants on a soil bank, run against its own ablation (`PIXEL_PHYSICS_BURROW_LINING=off`), which is the baseline a *standing* quantity has no other way to get. It carries the correction that matters to anyone censusing a dug volume: **a rectangle census of empty cells measures erosion, not excavation.** Digging destroys its spoil, so every cell removed lowers the bank somewhere, and the empty rows that opens at the top of the footprint score exactly like a chamber -- measured 2026-08-30, the ablated colony read **788 standing void** against **803 cells of soil gone**, the same number twice, with not one enclosed cavity in it. The columns that state the claim are `roofed` (empty with ground standing above it) and `roofed3` (three cells of it), neither of which lowering a surface can move: 130-151 against a flat **0** across four seeds. `map=1` dumps the bank as ASCII, because `roofed` cannot say whether its cells are one gallery or forty pockets, and `crop=`/`zoom=` steer the sheet -- at zoom 6 over a whole bank a one-cell gallery is six pixels of brown inside brown. **Since 2026-08-30 it also answers the two questions `roofed` raised and could not settle**, and both generalise a long way past ants. `comps`/`largest`/`ge8`/`lgroof` plus a printed size distribution are a **connected-component pass over the standing void**, 8-connected because that is the neighbourhood the digger itself uses -- 130 cells as one gallery and 130 cells as 130 separate bites are the same `roofed` number and opposite findings, and the answer is that **91-96% of enclosed void is one connected run of 134-165 cells with 0-2 singletons**, so the colony tunnels and never nibbled. `lgroof` is carried beside the size because a quarried face is one enormous run with no roof over it and would otherwise score as the best tunneller in the sweep. The general form is that *any* census of a total wants its component distribution before it is read as a structure. And `contrast=1` is the **render-side** half: it samples the shipped `Renderer`'s own output buffer, one pixel per cell, and reports mean colour and the p5-p95 luminance spread of each class in the bank, with open sky as a positive control (178.4 at **zero** spread) and the ants' own open-face bite as a second (158.3, correctly indistinguishable from sky). It answers *can a player see X against Y* for any two things in this engine, which nothing else here could, and it cannot be answered from the palette `.ron` files because what reaches the screen is what lighting makes of them: the tunnel interior measures **86.4 mean over a p5-p95 span of 119 of 255**, the only class in the scene with no colour of its own, while soil against the packed lining is the one separable pair and clears the bank's mottle by a hair (9.4 against 8.5). `light=4|2|1|depth` drives `Renderer::sky_light`, which is what ruled the lighting resolution out as the cause -- exact per-cell light makes the tunnels *brighter*, not darker | **`floats`/`fcells` are the 2026-09-01 addition and they carry a correction to `roofed` itself**: pieces of ground with no path down to the floor, by flood fill, which is the count behind an owner's *"there's a lot of dirt just floating in the midair"*. The per-cell version of that question -- ground with nothing directly beneath it -- reads **49-62 on the shipped behaviour** and is almost entirely gallery roof, which is unsupported by definition; only the component pass separates a hanging speck from a working tunnel. And the correction: `roofed` counts a void cell with ground *anywhere above it in the column*, so a handful of floating crumbs roofs everything beneath them -- removing them took the shipped colony's own lab-bed nest from 197/179 to **110/83**, which is the instrument becoming honest rather than the world getting worse. Read `roofed3` beside `roofed` for the same reason. **The 2026-09-02 addition is `circ`/`inradius`/`buds`, and it is the same correction one level up again**: `roofed` and the component split are both *volumes*, and a round chamber and a ramified warren of equal size are the same number and the opposite finding -- which is the whole of what Toffin's density-dependent digging predicts, so the mechanism could not have been judged without them. `circ` is `4*pi*A/P^2` over the largest **roofed**-void run (roofed, because a quarried open face is one enormous ragged component and would otherwise be reported as the colony's chamber), `inradius` is the largest disc that fits inside it, and `buds` counts runs standing further out than `BUD_K` times that radius. **`arms=selftest` is the part that generalises**: six synthetic shapes with known answers go through the same function, printed and asserted, and it caught two defects in its own instrument before any colony number was quoted -- a first-wins tie-break putting the "centre" of a uniform gallery at its end (one bud where there are two), and the doc comment's claim that `circ` ranks a disc above a square, which on a 4-connected grid it does not: a staircase perimeter costs a curve every step, so a digital disc reads **0.572** against a square's **0.785** and the column is *compactness*, not circleness. Any shape metric written in this repo wants that arm. `crowddig=`/`digbias=` patch the two genome slots before the ants are planted, so an arm and its own ablation run from one binary on one set of worlds. **What that machinery was built for came out a null** -- `(Crowding, Dig, 0.6)` reads 4 of 4 seeds more compact on four seeds and **16 of 33** on twelve, and is in `dead-ends.md` rather than in any species file. **And running twelve seeds is what found this arm's own worst bug:** founder placement was `off = (seed - 1) * 3`, which walks the row rightward until every ant is inside the bank, so **seeds 8-12 ran 8,000 frames with `digs 0` in both arms** -- an empty scene that reads as *"the effect disappears at larger samples"*. Placement is a bounded per-seed shuffle now with the founder count asserted, and **every number this arm produced before 2026-09-02 was measured on at most seven distinct colonies**. The general form, which is not about ants: a harness that derives a scene parameter from its seed by *translation* eventually walks out of the region the scene is valid in, silently, because the run still completes.
| `lab_resolution` | **How much resolution can the evolution lab afford?** The real `LabBox` bed at 1x/2x/4x linear cell density, with `field::FIELD_SCALE` as the second axis | Built for `Reports/evolution-lab-gui-physics-2026-08-30.md` against the owner's own hypothesis -- *"could we have a different game resolution and field resolution"* -- which `resolution-step-2026-08-29.md` predicted at "~4x cheaper" and nobody had measured. **Two things in it are the reason to reach for it rather than `labbox_cost`.** It builds `lab::scene::LabBox`, which has a stone **ceiling**, where `labbox_cost`'s `PlantScene` is open-topped under a held sky -- the ceiling is precisely why the lab's field does not solve every tile every frame, so the two are different field workloads and the resolution question has to be asked of the real bed. And it scales `ground_y` with `height`, which neither `labshot` nor `labbox_cost` exposes: at `height=640` with the default `ground_y=160` the soil sits in the top quarter and 390 rows are empty void, which is a scene error wearing a result. **The counter trap it documents generalises to any `FIELD_SCALE` work**: `solved/f` counts `FieldTile`s and there is exactly one per `Chunk` at *any* `FIELD_SCALE`, so it is **invariant by construction** -- reading it as "the work did not move" or as "the work vanished" are both wrong, and the quantity that actually moves is `solved/f * FIELD_TILE_AREA`, printed beside it. Every line stamps `fs=<FIELD_SCALE>`, because the harness is as stale-able as the constant it was compiled with and there is no other way to tell two builds' logs apart. `fans=1` is the positive control; `mode=render` and `mode=shot` answer the visual half |
| `labsoil` | **Does a deeper bed cost the stand?** The real `LabBox` at N depths x M world seeds, censused at **equal ticks**, counters only, no rendering | Built because one paired run said deepening the bed 40 -> 96 rows cost 20% of the stand. **It does not reproduce**: 4 depths x 12 seeds x 9,000 frames puts the direction neutral-to-slightly-positive at every depth, and the single run that condemned the change (ratio 0.80) sits inside its own arm's spread of 0.81-1.57. That is the whole reason it exists, and the general lesson is `CLAUDE.md`'s: **a single paired run is a sample from a wide distribution**, and the seed knob here is live (306-748 cells at one depth), so a null is not a dead axis. **Three hypotheses were closed by measurement and the order they fell in is worth knowing**, because the plausible one was wrong. *Light* was suspected first -- an earlier session had thickening the ceiling 4 -> 7 rows costing 45% of the bench light and half the stand -- and it is **ruled out and cannot happen**: light at the bench reads 0.447 in all 48 runs, identical at every depth, because soil sits *below* the bench and the sky walk attenuates through what is above it. **The `0.216 -> 0.172` that started the hunt is downstream of the stand, not upstream of it** -- a bigger stand shades its own bench -- which is a reversed-causality trap worth carrying to any bed change. *Water* is ruled out with the sign the other way: a deeper bed is built at field capacity and drainage runs only above it, so whole-bed available water *rises* with depth. *Frame cost* is real and is the honest cost -- about **+23%/tick** on a young box, paired, alternating, slower in 5 of 5. **And it answered the design question the depth was for**: roots max out at **13 rows over all 48 runs** and do not use what they are given, while the colony's galleries were already at **35 rows of a 40-row bed** -- the bed, not the biology, was the binding constraint on the burrow system. Deep soil is earned by the ants, not by a hypothetical deep-rooting mutant |
| `labui` | **What the lab's control bar looks like, without a window** | The only way to judge the bar headlessly -- `labshot` renders the *world* and shows no interface, and the app's own screenshot hook needs a display. `Ui::widget_rect(action)` is how it aims a click, so "click where the button ought to be" is unwritable and a test cannot drift from the layout. **The width finding is the one to know before editing the bar**: at seven speed stops it fits at exactly **508 of 508 px** at natural spacing, `SPACINGS` retries at tighter padding, and `PIXEL_PHYSICS_BAR_TRACE=1` prints the width at each rung -- so one more stop or one longer label drops it to the cramped layout. `PIXEL_PHYSICS_BAR_TRACE=1` prints **per row** since 2026-08-30 -- it printed only the last widget's right edge, which is row 1's, so a row 0 that had overflowed was invisible in the very trace you read to decide where a new button goes. Measured with the parameters button added: row 0 fits at 506 of 508, row 1 at 508 of 508. It also drives the **parameters page** end to end -- every `+` face on every page is clicked at the rectangle it was drawn at and the value read back, so a row that paints correctly and refuses to move is named rather than photographed, and `save_check` reports which rows can be written back to an asset file without editing one. `split=1` writes one tile per file at full size and `only=SUBSTRING` picks tiles by title: a twenty-tile sheet is 1,536 px wide and every viewer scales it down, which is the wrong thing to do to a page of 5x7 glyphs |
| `labnest` | **Does a colony's nest stand in the lab's own bed, and are the ants stuck or dead?** The real `LabBox` with a colony in it, censused over time | Built to reproduce an owner playtest report -- *"tunnels the ants are digging are collapsing or soil is filling and they get stuck underground"* -- which `burrow_probe` could not answer because its scene is a hand-built bank and the lab's bed differs in three ways that each bear on it. **The structure half does not reproduce**: `roofed` void rises to 230-260 and holds, the lining holds at ~750, and `buried` never exceeds 4 over 9,000 frames. **What is real is a die-off** -- ants 52 -> 12 between frames 4,000 and 5,000 in both seeds, with `digs` plateauing immediately after, which from outside looks exactly like getting stuck. **Two things in it generalise.** It reports the *margin* rather than the outcome: `overcap` counts wall cells past the un-pack threshold and `wettest` prints the worst reading, so a bed sitting one unit under the line reads differently from one sitting a hundred under it -- and that is what killed the leading hypothesis, since the lab bed is built at exactly `SOIL_FIELD_CAPACITY` against a rule that fires strictly above it. `overcap` was **0 at every frame in both the default and a `dry=1` control**. And it splits the report into the two halves that have different fixes: *collapsing* is `roofed`/`packed` over time, *stuck* is `buried` (a body walled in on every face) plus `blocked%`, because an ant walking a gallery and one entombed both read "below the surface". **Its own caveat**: the default run has `founders: 0`, so there is no food and starvation is confounded with burial -- separating them is the next measurement, not a conclusion it makes |
| `field_sense_probe` | **Does a sense read anything when the animal is underground, and does the moisture channel read *curvature* at all?** Two channels over two scenes: a hand-built control bed carrying three curvatures in one elevation band, and the real `LabBox` with a colony in it, every ant classified by whether it has ground over its head | Built for `Reports/creature-genome-flexibility-2026-09-02.md` §10 Stage 0, and its first finding is about the channel rather than about burrows: **`moisture_gradient` does not distinguish a convex crest from a flat plateau at the same elevation** -- 1.012x at the shipped ±4, and 1.011 / 1.006 / **1.003** at ±8 / ±16 / ±24, so widening the sampler to match `FIELD_SCALE` moves it *toward* 1.0 rather than away. What it returns is the vertical air/soil step, which every surface in the world has: the termite drop bias is a **depth** bias, not a curvature one. **Three designs in it generalise past ants.** The scene holds curvature and elevation apart by construction -- an earlier version compared a crest against a plain 30 rows lower and could not say which of the two it was reading, which is the confound any "does shape matter" question has. It carries the **instrument's own positive control separately from the claim** (depth must move the reading, measured 1.74x) so that a flat curvature result is a statement about the world and not about the probe -- the distinction `CLAUDE.md` records six occurrences of. And its `dx`/`dy` split is what makes the null readable at all: **do not put a bar on `dx`**, and this cost a wrong verdict before it was caught -- the apex of a symmetric ridge has `dx = 0` by symmetry rather than by dryness, which reads as a clean 0.09x separation and is an artifact. It calls the shipped `creature::moisture_gradient` and `creature::sighted`, never a copy, and prints `FIELD_SCALE` in its own header because every finding it makes is a comparison against it |
| `spoil_curvature` | **Two questions asked before a lever is built: does the placement rule this lever feeds admit the sites the lever prefers, and does the sensor ever visit both ends of its own range?** Enumerates every cell `act`'s spoil-drop predicate accepts in a real bed, and samples signed terrain curvature at ant head positions *through* a run rather than at the end of it | Built for `Reports/creature-genome-flexibility-2026-09-02.md` §5f, and it is the machine form of §14i's lesson: a lever is null **by construction** if the response is unreachable or the sensor never enters the regime the model is about, and both are cheap to check first. **Three things in it generalise past ants and past curvature.** It reports **deciles and the share at each extreme, and refuses to print a mean or a max** -- those being exactly the two statistics §14i records being reported instead of the ones that would have caught a saturated sensor, and a reader handed them will use them. Its **control runs first and asserts an ordering it knows** (crest > flat > notch, separated by 0.5 each way) so that a flat distribution is a statement about the bed and not about the estimator -- and that control earned itself on its first run, catching a five-wide slot reading as *convex* because a radius-2 disc spans five columns and saw no wall, which is a real limit of the sense rather than a bug. And it runs **two beds**, which is the finding: `bed=lab` reads p10 = p50 = p90 = **+0.000**, zero spread over 18,720 samples, because `LabBox` lays a level bed; `bed=bank` reads p10 -0.250, p50 +0.417, p90 +0.833, **spread 1.083**. Measuring only the first and reporting the channel dead would have been §14i's own first retry condition -- *the scene may not contain the phenomenon* -- committed by the session quoting it. It also prints the **body-counting variant beside the terrain one**, because the difference is a diagnosis: counting flesh returned exactly `-0.083` at every one of 18,720 samples, which is flat ground plus the ant's own second body cell, and a perfectly constant result across twelve chaotic seeds is the tidiness tell rather than a strong effect |
| `creature_arena` | **Does this world punish an animal that is worse?** Two genomes race in one `LabBox` for the same plants, water and space; attribution by `OrganismState::lineage`, read at a seed count. **Since 2026-09-05 it asks the general form of that question, and both halves of it were missing.** `arm=ablate input=<sense> [output=<verb>]` removes an arbitrary sense or a single brain edge rather than only the four hard-coded rungs, so *does this environment select for X* is one command for any X; and `digcost=`/`emitcost=`/`spoilweight=` vary the **economy** in the same run, because an arena that can only vary the genome answers half the question — the paired control (same ablation, priced world against free one) is what separates a selective pressure from a coincidence. Worked in `Reports/selective-environments-2026-09-05.md`: the dig drive reads a **median 50.0% null with every verb free and 63.6% on 4 seeds of 4 at the shipped prices**. **Its limit is separability, and it will not warn you** — check the weights you ablate remove the behaviour *and nothing else*, and check it by tracing what the pathway is *worth*, not by noting which output it reaches. Both readings of `arm=notrail` are worked in that report: the shallow one ("the pheromone senses reach the units that drive `Move`, so this removes navigation") is **wrong**, because those units are differential pairs that cancel exactly at zero signal, leaving `Bias -> Move` intact — and the arithmetic that shows it also found the real finding, that the trail pathway is **saturation-gated on carry state**, worth 18% of the `Move` decision at ~60% laden and under 1% empty or full | The creature **Gate 2**, which did not exist -- `selection_arena` asks this for plants only, and the lab coordinator note has carried *"Gate 2 has still never been run... until it passes, every evolution result measured in this bed is unvalidated"* through five rounds. Built for `Reports/creature-genome-flexibility-2026-09-02.md` §9. **Its first run produced the finding that governs every later one, and it is about horizons rather than about ants**: at `labbatch`'s 9,000-frame default the `arm=lethal` arm -- a brain with *every weight zeroed* -- read **65.8% of the animals on 4 seeds of 4**, i.e. it beat the authored ant, with 52 of 52 alive at generation 0. It had won nothing. An ant's founding grant is `start_energy / (idle_cost_per_cell x cells)` ticks = **12,000 frames**, so inside a 9,000-frame window not spending is strictly better and starving is not yet possible. At 24,000 frames the same arms read **0.0% on 4 of 4** and the bed has teeth. **A negative control that cannot lose inside the horizon is not a negative control**, and the harness now computes that grant from the species file and prints which side of it the run is on -- the reusable form being that any harness racing arms of anything endowed with a stock must outlast the stock, or it measures who was given more rather than who earned more. **Three further designs transfer.** `arm=` is a ladder (`same`, `lethal`, `nofeed`, `notrail`, `random`) so it reports *where* discrimination stops, and it **refuses to run when the edit matched no live slot**, because two identical arms read as a clean 50/50 -- indistinguishable from the finding it exists to make. `mirror=off` is not an option but the control: mirrored, `arm=same` is one simulation with the labels swapped, so its exact 50.0% is an algebraic identity and says only that the harness runs. And attribution is by lineage and **never by position** -- the one place the plant harness's reasoning does not transfer, because animals mix within a few hundred frames while two plants stay where they were planted |
| `skylight_cost` | **What the renderer's sky-light pass costs in an enclosed world, and whether it is still doing its job** — the lab bed drawn every tick through the shipped `Renderer` with the dirty-rect skip live, with the draw's mean/p50/worst beside the three routes a sky-light rebuild can take (`full` / `incremental` / `held`) and how many of the solves actually moved a byte of the grid | Built for the sky-light block cache (`Reports/lab-sky-light-cost-2026-08-30.md`) and generalises to **any cache whose correctness rests on `World::take_touched_chunks`**. Two designs in it are worth lifting. The counts sit beside the timing because *a cost that vanishes may be work that vanished* — `held` climbing while `changed` stands still over a box that is visibly growing is a broken invalidation wearing a speed-up. And **`check=1` is a positive control in the shipped bed rather than a hand-built miniature**: every `checkevery` frames it draws the same world twice, once through the `Renderer` that has been running and once through one made a moment ago, and compares **the light grid as well as the pixels** — which is the half that discriminates, since a stale block deep in a dark room moves the grid by a byte and moves no pixel at all (measured: with the cache's change detection deliberately broken, `labshot`'s contact sheet came back byte-identical). Its bar is the dirty-rect skip's own rather than perfection: a chunk written after `World::end_step` reaches `touched_chunks` only on the next tick, so a one-frame difference is shared with the pixel path and only a *persisting* one is a fault |
| `film_probe` | Standing census of one-cell water films | Standing count, not creation rate — the distinction that solved the whisker hunt |
| `fire_probe` | The grassfire instrument | |
| `anchor_probe` | **Does it matter which code path last wrote a field?** One geometry, its anchor distances written three ways, swept for the margin | Built for `open-bugs-handoff.md` §S2; the shape generalises — see below |
| `arch_probe` | **Does the shape a player builds change what stands?** One opening, four roof forms, swept for the span each one drops at | Builds and runs scenes; changes no default. Refuses to print a margin it did not bracket — see below |
| `support_census` | **What the support field is made of, and what a replacement would cost.** The distance histogram, the coarse chunk layer's true node and edge count, and a cell-by-cell comparison of a candidate field's *load DAG* against the exact field's | Read-only — builds candidate fields beside the real one and never writes. `control=1` runs both controls; see below |
| `wg_ceilings` | **What worldgen cannot express, as opposed to what one world came out like.** `mode=step` is the largest single-column skyline step in the *finished* world, ablated per pass to attribute it; `mode=region` is where region boundaries actually land as a fraction of world width; `mode=strata` counts how many distinct rock hardnesses a whole world has; **`mode=relief` is the three numbers W1 is judged on in one command** -- how far the skyline moves across one player screen, how tall the tallest thing at formation reach (15 and 30 columns) is, and what share of on-screen ground lies within six cells of air, with the starved landform passes' cell counts beside them | Read-only, changes no default. **`mode=relief` prints two prominence statistics and they disagree on purpose**: `viewshot`'s two-sided prominence scores a spire and scores **zero** on a scarp, a bench rim, a mesa edge and the whole interior of a plateau, so steering by it rewards exactly the tall pillars the owner rejected; local relief (the skyline's range over the same window) counts any vertical structure at that width. It also prints the **denominator** beside the near-air share, because relief moves both terms of that ratio and the share can fall while the boundary has grown. Pairs with `PIXEL_PHYSICS_RELIEF=0` for a same-binary A/B (`Reports/worldgen-relief-2026-08-30.md`). **`mode=step` generalises: it is the check that catches a terrain claim measured against the plan heightfield rather than the world** — the two differ by an order of magnitude, because `residuals` paints vertical faces after the plan is fixed. Prints one tagged line per measurement, because the passes `println!` during generation and a `print!`-built table interleaves with them |

## Creatures

| instrument | answers | notes |
|---|---|---|
| `creature_probe` | What a creature is sensing and deciding, per tick | **Since 2026-08-30 it also answers "is this population actually dying", which is a different question and had no instrument.** It prints *two* counts of how many animals are left — head cells standing in the world against entries in the organism registry — and the gap between them is the whole of `open-bugs-handoff.md` §R3: a colony reported extinct was alive, because everything that finds an ant looks for a `CellType::Head` and a `Chain(n >= 3)` loses that marking. Pair them with `bodies ever built` (`stamped / body_energy / cells`, a **placement** counter that does not go through peak population) and `built - deaths = registry` becomes a checkable identity — the cheapest way to tell a death from a bookkeeping hole in any population run. The **energy-ledger accounts** are printed unconditionally and partition where flesh went three ways: `harvested_corpse` is scavenged, `meat_lost` is destroyed-without-being-eaten (including living flesh bitten off), `stamped` is created. `chain integrity` and the standing **cell-type histogram** are the body-level pair, and they generalise to any organism with a multi-cell body — a plant losing tips reads the same way. Knobs: `body=`/`idle=`/`move=` run a body against the bill it used to pay; `pitch=` is the founder spacing, which is a **confound** in both scenes rather than a parameter (the shipped 2 was calibrated to the 2-cell ant, so a 3-cell body overlaps its neighbour: 28 founders placed against 46 at `pitch=4`); `kinfood=off` makes living kin flesh worthless and `eatskin=on` is its positive control, the pair that ruled cannibalism out. `seed=` takes hex as well as decimal, because the probe echoes hex and §R3's own quoted command panicked on it |
| `creature_probe` | What a creature is sensing and deciding, per tick | |
| `stamp_probe` | **What would it take for this ant to afford a child?** Prices a birth against the bar (`birth_grant` + `body_energy` x cells) and against the bank ceiling (`hunger_fraction` x `start_energy` + best mouthful), and runs the colony to see whether it breeds | Built to price the three routes past the body stamp, and **two of its three columns generalise past reproduction**. `gut=` sets the diet gene, which `creature_probe` cannot, and reads it back off a **live founder** -- a species-level write that did not reach the founders would otherwise leave every run silently measuring the neutral gut. **It prices the best mouthful over the food *standing in the world*, not over the material table**, which is the correction to `creature_probe`'s stated trap ("can rule out and can never rule in"): measured at `gut=-1.0`, whole-table ceiling **1,540** against **580** for what is actually there, because the 1,440 `flower` that makes the table look generous exists in no sampled world at frame 600 or at 24,000. `placed=` says how many of the 55 founders got onto the ground -- **2 on seed 1, 51 on seed 21**, and a world with no ants in it reports `births 0` exactly like an ant that cannot afford one. `body_energy=` is a stamp proxy: it moves `ant`/`corpse` `food_energy` with it, holding the flesh-pricing invariant rather than opening the pump it would then measure. **Its known limit, measured 2026-08-30: "standing in the world" is still not "reachable".** It counts a flower 22 to 40 rows up a stem exactly as it counts a leaf on the floor, so it fixes one half of `creature_probe`'s trap and inherits the other -- `evolution-lab-gate-1-2026-08-30.md` §4.3 read a margin of +500 and got zero births on that gap. Pair it with a census of what is on the *ground* before concluding an economy is solvent. One `SUMMARY` line per run, for sweeping |
| `creature_scale` | **Is this animal the same *physical* size in a world built at a finer grid, and can it still walk?** `mode=size` renders one body per resolution cropped to a fixed number of **physical units** and upscaled so every panel is the same pixel size; `mode=walk` is `moves_blocked / (moves + moves_blocked)` for a colony on generated terrain | **The `2x, scaling off` arm is the design worth stealing, not the subject.** It reads the authored `CreatureDef` out of a 1x world and writes it into a 2x one with `SpeciesRegistry::set_creature`, so the two arms differ by the scaling pass and **not** by the terrain a different grid generated -- the general remedy whenever an A/B's obvious form would vary two things. Its bounding box is printed twice, in cells and divided by `k`, and only the second column is the claim: a body with four times the cells at half the size passes a cell-count assertion and fails this one. `mode=walk` generalises past resolution -- it is the **body-plan mobility** instrument, and its `Chain(2)` row is a standing positive control at **5.2%**, against `creature-appearance-design.md` §5's 5%, so a run that does not reproduce it is measuring something else. Its own first run reported `moves=0 blocked=0` for every arm, a clean null from a loop that called `parallel::step` alone: creatures live on the active-site scheduler, so any harness of this shape needs `step_active_sites` too. Crops to physical units rather than cells, pins daylight, and echoes its own parameters. `Reports/creature-cell-scale-2026-08-30.md` |
| `creature_body_probe` | **Does a rigid body's blocked-move rate depend on its width, its height, or its cell count?** Holds every economic field to `ant_block`'s and varies only `body` (`SpeciesRegistry::set_creature`, `creature_scale`'s own control-arm technique), so whatever moves is the footprint | Built to give `creature-genome-flexibility-2026-09-02.md` §13e's "no measured blocked rate for a 2x2 rigid body anywhere" a number without building the multi-part `BodyPlan` the report proposes. Finding, 12 seeds each on `rolling`: footprint **width**, not height or cell count, sets the rate, and sharply -- a footprint <=2 cells wide (`domino_v` 1x2, `domino_h` 2x1) blocks 8-10% median regardless of height, a footprint >=3 wide (`strip3` 3x1, `strip4` 4x1) blocks 47-53% median regardless of height, and the step lands between 2 and 3 wide rather than scaling smoothly with size. Also carries `forward_taper`/`backward_taper`, the same 14-cell composite silhouettes `creature_candidate_render` renders, measured here as one monolithic `Rigid` body: both land at ~54-57%, statistically indistinguishable from each other and from `ant_block`'s plain 3x3 -- the control a genuinely decoupled body's position-dependence claim needs, since a monolithic body cannot benefit from a trailing part reusing cleared ground and this is what that floor costs. `Reports/creature-shape-reachability-2026-09-02.md` |
| `creature_shape36_probe` | **Does shape at constant cell count move findability once the body is big enough to plausibly read as a shape rather than a smudge?** Reuses `creature_look.rs`'s paired with/without render and its `ink`/`decoys` method (duplicated, not imported, so that file is untouched) | `creature-appearance-design.md` §4 found two 9-cell bodies (a filled 3x3, a waisted 5x2) 0.8% apart on `ink`. This re-runs the same design at 36 cells (a filled 6x6 against a three-segment waisted "insect" silhouette, head/waist/abdomen), because the owner's "it is a perfect cube" verdict was a shape reading delivered at 36 cells and the appearance report's own §13d in `creature-genome-flexibility-2026-09-02.md` predicted the two findings sit on opposite sides of a threshold -- what this instrument measures is the findability operationalisation of that prediction specifically (`ink`), not whether a body *reads as an animal*, which stays open per the report's §3 correction. Measured: still ~0.5% median across 5 seeds (range 0.1-1.6%) on `ink`, not the ~15% a threshold crossing would predict -- `|contrast|` is noisy (2-22%) exactly as the original report found it already inside the noise at 9 cells, and for the same reason (the two shapes cannot share a placement, so their surrounds sample different terrain). **A third arm, `narrow36` (`block(2, 18)`, the mobility-safe width §1.3 of the same report measured), asks whether extent grown as length rather than as a compact block keeps the same findability** -- it does, within the same noise band as the first two arms on 5 of 6 seeds (one placement-driven outlier). `Reports/creature-shape-reachability-2026-09-02.md` |
| `creature_candidate_render` | **What would a candidate articulated-body silhouette look like, rendered at shipped resolution?** Stamps a single `Rigid` body shaped like each candidate -- a static silhouette does not depend on whether its segments are one rigid plan or several parts following each other, only movement does -- at `creature_scale mode=size`'s own crop and zoom, one PNG per shape | Built because there is no multi-part `BodyPlan` to render and building one is out of scope for a measurement session. Ships `uniform3`/`uniform5` (3 or 5 equal pinched segments) and `forward_taper`/`backward_taper` (small-head-big-abdomen and its mirror) alongside the shipped `ant`/`beetle` for reference, for posting as a blind gallery rather than reading as a metric. `Reports/creature-shape-reachability-2026-09-02.md`, review card `20260902T194120383Z-3860b1` |
| `creature_space` | **How many distinguishable ways of being an ant does this system admit?** | |
| `ant_ablation` | Is the authored brain doing anything, or is it the substrate? | The control that separates a mechanism from its scaffolding. **~15 min at its defaults** (20 arms x 5 seeds x 6,000 frames, ~1.39 ms per arm-run-frame, measured 2026-08-29 on 4 cores); progress goes to stderr. Its defaults answer the *locomotion* question only — `deliv` and `eats` are 0.0 in every arm on a corpse pile. Use `terrain=world food=trees` for anything about foraging |
| `forage_probe` | Does the colony actually range, and how far? | |
| `windfall_probe` | **Does the food ever reach the floor, and does anything ever reach the food?** Builds `lab::scene::LabBox`, censuses the flower -> fruit -> windfall pipeline against `World::fruit_dropped`, and reports what the colony was offered, what it swallowed, and how rich it ever got | Built because *"windfall never exceeds one standing cell"* is **three findings wearing one number** -- few are produced, they are eaten instantly, or they decay -- which want three different fixes and which nothing here could separate. **The separator is Little's law**: production and standing stock are counted separately and `mean standing / production rate` is the mean time one *stands*, so a stock of one cell is readable as "one made every 400 frames lasting 300" or "four hundred made each lasting one frame". That trick is not about fruit and closes any "is this stock small because nothing makes it or because something eats it" question where creation is counted; `colonies=0` is the sink control beside it. **The census is by organism rather than by grid sweep** -- every cell it cares about is organism-owned -- which is what makes sampling often enough for Little's law cost a few hundred lookups instead of 163,840. **`handout=` is the positive control and is the point of the binary**: it drops food on the colony's doorstep and changes nothing else, which is how the shipped ant was shown to breed to **generation 13** while breeding zero times in the same bed unfed. Also counts windfall by **height** (food lodged in a canopy is not food on the floor), censuses the nest larder through `food_value`, and echoes `png=`. **Founds its colonies after setting the gut, never before** -- `place_creature` copies the trait at placement, so a species-level write to a standing colony reaches nobody and the run silently measures the neutral gut |
| `predation_probe` | **Can a beetle smell an ant?** The T6 pre-flight: how much channel-A and channel-B pheromone there is world-wide, what fraction of prey and of predators stand within a sensor offset of a nonzero cell, and how far a beetle is from the nearest one | Answers a question no fitness harness can: `creature_space` reports per-ant survival on the same scene and `creature_probe` reports one creature's inputs per tick, and neither can say what a *plane* looks like world-wide. Generalises past predation — `mode=preflight` is a **stigmergy census** and would answer "is there a trail where the ants are" for any signal-following species. `mode=control` is the positive control in the same binary (paint a saturated trail; stand a beetle on an ant), `mode=ab` re-measures the `beetles=0`/`beetles=9` null, `mode=cost` times a whole frame on a settled world. **`mode=range` asks a later and different question** — *does predation kill non-randomly with respect to how far an ant ranges from home*, which is what decides whether a predator is a selective force or a flat tax. It diffs creature identities between samples to place each death in a distance-from-nest band, reports that against the standing distribution, and runs the whole thing again with `beetles=0` so starvation's own distance bias is subtracted rather than assumed. It prints its coverage against the engine's death counter — 1.00 measured — which is the check that says the diff is counting dying rather than something else. Generalises to any "does this mortality select on that behaviour" question. Echoes its own parameters |
| `vision_probe` | **How far would a predator have to see?** The E15 pre-flight: what fraction of beetles have a prey animal in unobstructed line of sight at radius 8/16/32/64, what terrain occlusion costs that fraction, and what a ray fan costs per frame | **Not predation-specific and not really about vision — it is a line-of-sight geometry engine over `World::get`**, so it answers *"can A see B, and what is in the way"* for anything the lattice holds: whether a gnome has sight of a cave mouth, whether a plant tip has sky, which material is shadowing a cell. The pairing that makes it readable is `range` (there was something in reach) against `los` (it could be seen) — the gap between them is occlusion and nothing else, and a probe printing only the second cannot tell a reach failure from an occlusion one. `blocked_by` names the *material* that stopped each ray, which is what turned "occlusion is expensive" into "it is seeds and litter on the floor, not hills". `mode=control` runs three arms with both halves of the check — `clear` proves the sight test reaches 1.000, `walled` proves it falls to 0.000 **at unchanged `range`** — after asserting the tracer on a hand-built world. `occl=none` is a transparent-world **ceiling** every other arm is asserted at or below, free on every run. `mode=cost` carries a third arm, `locate`, which does the harness's own whole-world beetle scan and casts nothing: **the sense costs `rN` minus `locate`, and `vs blind` overstates it thirtyfold** — that arm also prices one `World::get` directly (15.6 ns) because it reads every cell once and does nothing else, which is a number any other harness can borrow. `eye=`, `cone=`, `settle=` and `preset=` are the sensitivity axes; `settle=` exists because the creature placement is a confound and had to be ruled out rather than argued away. `mode=overlay` writes a cropped, zoomed sight-line picture through the shipped `Renderer`. Echoes its own parameters and panics on an unknown argument |
| `creature_look` | **Can you find this thing in the picture?** `ink` is how much luminance a body puts on screen that the ground would not have, from a paired with/without render; `decoys` is how many *other* places in the frame are at least as different from their surroundings as the body is | **Not creature-specific — it answers "is this findable" for anything drawn into this world**, which is the question `plant-appearance-design.md` had no instrument for. `decoys` is the one that explains a picture: contrast says a body differs from its background, and this says how many things in the frame differ just as much. Pins daylight (a luminance number sampled at an arbitrary hour is a statement about the hour) and builds on a `WorldgenPresets` preset rather than `build_terrain`, whose bare skyline makes every surround reading the sky. Echoes its own parameters |
| `motion_look` | **Would you notice this move?** The motion half of `creature_look`: a *still decoy* is that harness's `decoys` unchanged, a **moving decoy** is a still decoy in which a pixel changed between two frames `gap` apart, and both come out of one loop so `moving <= still` holds by construction | **The finding is that the decoy field is entirely static** -- a body that moves has 0-2 competitors at *every* size from 1 to 16 cells, where a still one has 141 at two cells. Generalises past creatures: it answers *is this thing's motion visible against everything else that is moving* for any drawn object, and its `weather=` arm is the general way to ask *what does a busy sky do to a visibility number* (rain and a storm move 2,870-6,316 pixels and produce **no** decoys; a blizzard produces 13, because snow both falls and settles). `mode=live` adds the target-side number a decoy census cannot give -- **how often the animal is moving at all** (22-42% of ants never move across a 384-frame horizon). Carries four controls, three of them asserts: a frozen pair must change 0 pixels, the counter must fire on every window at contrast 0, the motion axis must reduce to the static instrument at motion 0, and a probe moved one cell must be flagged while the same probe held still must not. Pins daylight and echoes its own parameters. `Reports/creature-motion-decoys-2026-08-30.md` |
| `gnome_depth` | Does the gnome weave *through* a formation, or get sliced *by* it? | |
| `species_export` | **Can this individual be kept?** Writes a genome + traits out as `assets/species/<name>.ron` and reads it back through the loader | Not a measurement — the dev-tool exit for E8. `genome=rNNN` takes the same labels `creature_space` ranks, so a good row in that sweep can be saved as a species. `verify=1` (default) is the round trip |
| `larder_probe` | **Is there a standing pile of food beside the nest, and is it a store or a flow?** The pre-flight for `store_in_body` (`larder-reachability-2026-08-30.md`): free food banded by Chebyshev distance to the nearest nest cell, priced in what the ant's own gut can digest rather than in face value | **Not larder-specific — it is a *proximity census with both controls in the binary*, so any "is this quantity concentrated at X, or merely present in the world" question is a run rather than a build.** That is the recorded failure it is shaped against (a census counted every `Solid` in the world rather than the platform under test), and `mode=control` settles it by planting the same pile at the nest and at the far end: the banded column must move for one and not the other, while the world column moves for both. Three things generalise. **`mode=turnover` tracks the band as a *set of positions*** and reports entries, exits and residents — the distinction a standing count structurally cannot make, and the one that turned "a granary of ten cells" into "ten cells in transit" (`resident` 0 from frame 200). **The 2x2 of `ants` x `planted`** measures persistence *without depending on the mechanism under test* — a hand-planted pile answers "could a store stand here" even in a world where nothing builds one, which is worth copying wherever a null might mean either "the verb does not fire" or "its product does not survive". And **`mode=pair` renders the colony against a colony-free world from the same scene the census ran on**, rather than reaching for `filmstrip`'s `scene=colony`, which builds a different nest. Echoes its own parameters and panics on an unknown argument; daylight is pinned in both render modes |

## The evolution lab

`labshot` is the other half of this pair and is listed under *Rendering and
judging by eye* above: it answers **what the bed looks like**, this answers
**what is happening in it**. Reach for both — a founder that never germinated
and one too small to see are the same picture and opposite findings.

| instrument | answers | notes |
|---|---|---|
| `seedbed_probe` | **Can a seed start on what a dead plant leaves behind?** N seeds sown onto a mat of one material laid across the lab bed, against bare soil, counting germinations | Built for the owner's report that *"the ground gets covered in deadwood and new seeds cannot germinate in deadwood and everything dies"*. **It reproduces**: 16 of 16 seeds germinate on bare soil and **0 of 16** on a deadwood mat. **Two of its columns exist because the first version was wrong in both, and both errors generalise.** *`estab` counts organisms with more than one cell*, because an ungerminated seed **is** a live organism with one cell -- the first run reported 168 "plants" on bare soil of which 143 were seeds that had done nothing. And *`resting on` names the material under every surviving seed*, which is the scene check `CLAUDE.md` demands: a patch-width mat scored as permeable because seeds are `Powder` and **roll off it** onto the bare soil beside it, so the mat arm was not measuring the mat. **The mat-standing triple (laid -> at sowing -> at end) is the column that carries the finding** and is the one to copy into any probe over a decaying obstacle: litter reads a clean 16/16 and is *not* permeable -- 71% of its mat had rotted before the seeds landed (1,506 -> 436), while deadwood's was 85% intact (1,506 -> 1,285). Without that triple the two are indistinguishable and the wrong material gets blamed. **Its sharpest result is a negative one**: a mat of *soil* -- penetrable, water-holding, the thing one would convert debris into -- blocks just as totally (0/16, dry after 6,600 frames), so the gate is **water, not material**, and "turn the debris into soil" is measurably not a fix. Adding `water_capacity` and a low `penetration_resistance` to the debris does not fix it either (still 0/16, available water 0.000): fresh ground is created dry and does not wet from the damp soil below. Prints the species' root `penetration_force` beside each material's `penetration_resistance` and `water_capacity` -- the gate's inputs rather than its verdict -- so a null says *which* gate closed. Echoes its own parameters |
| `soil_wetting_probe` | **Does fresh ground ever hold enough water to germinate a seed on it?** One bone-dry soil cell over field-capacity soil, read as `soil_moisture` and `plant_available_fraction` at fixed stops, against the species' own germination thresholds | Built to settle a contradiction between two documents rather than to explore: `seedbed_probe`'s entry here and [`where-a-dead-plant-goes-2026-08-31.md`](where-a-dead-plant-goes-2026-08-31.md) both say fresh ground *"is created dry and does not wet from the damp soil below"*, while `update.rs`'s capillary rule is gated on nothing but `water_capacity > 0` and a gap above the rest threshold -- and a dry cell against field capacity clears that by an order of magnitude. **Both are right, and the reconciliation is the finding**: fresh ground wets *fast* -- it crosses grass's `soil_water_threshold` (0.10) at **frame 20** and herb's (0.15) at **frame 24**, reaching 0.445 -- and then, under the **full** pipeline rather than the CA driver alone, evaporation takes it back through both thresholds by frame ~500-600 and it settles at exactly `SOIL_WILTING_POINT` -- **available water 0.000** -- by frame 3,000 and stays there for ever. So the reported number reproduces and its stated cause does not, which makes the two documents' claim *true-but-for-a-different-reason* and puts the real constraint somewhere else: **a sealed bench dries out, and a seed's chance to germinate on any given patch is a window that closes rather than a state.** That is why the arm matters more than the number here -- `parallel::step` alone holds 0.445 indefinitely and `frame::step` does not, so a probe run on the CA driver would have reported the opposite answer with equal confidence. Decay-created and hand-placed cells are **bit-identical** at every stop (nothing in the rule reads provenance), which is the control that says the decay path is not special. **Its scene error is the reusable half**: the first version read a uniform zero everywhere because a 1-cell-wide soil column standing in open air is not a stable scene -- `soil.ron`'s `friction_angle: 33` is nowhere near the 90-degree slope such a tower presents, so ordinary powder movement slumped it before capillary had anything to measure. Every column is walled in stone for its full height so the only thing that can move `aux` is the mechanism under test; copy that into any probe over a per-cell soil channel |
| `reseed_probe` | **The whole reseeding funnel in the lab bed, stage by stage** — seeds borne, where each standing seed came to rest and *which* germination gate is shut on it, germinations, seedlings, established plants, and **how many of the bed's columns the stand actually holds** | Built for the owner's *"nothing spreads them"*, which is four claims about four stages that every existing harness collapses into one number. **`scatter=1` is its positive control and the point of the binary**: every seed the world creates is set down once on open ground at a uniformly random column, so dispersal limitation is gone and nothing else has changed -- it measures ~1.5x germination and ~1.3x coverage, i.e. dispersal is real and is not the headline. **`col=` exists because of a scene error that changes the answer**: `LabBox::spread(1)` puts a single founder at column 256, the darkest column in the bed, so a one-founder run is not a smaller eight-founder run -- 4 of 4 founders die there and 4 of 4 breed under a lamp. Prints the **bench light profile** every 8 columns, which is what found the 32-column dead bands. Reports the gate's *inputs* (resting material, its `water_capacity`, plant-available water, ambient light) against the species' own thresholds rather than re-deriving the verdict, and separates **on a seed pile** from **on plant tissue**, which are the owner's hypothesis and what is actually there. **Its `seeds borne` column is `World::seeds_borne` and had to be added to the engine**: two outside-in counts of the same thing are both keyed on the organism slot, which `push_organism` re-uses, so both miss the recycled births and *agree with each other* while doing so. Also prints the **bench light profile** every 8 columns, a per-frame cost line whose control arm is `founders=0` (no plants, so only the arm under test differs), and `World::germinations_in_place` -- germinations on an organism holding more than one cell, which a borne seed can never be, and which is what named `open-bugs-handoff.md` #Z4's mechanism. Echoes its own parameters. **`launch=N` is the 2026-09-03 addition and its seam matters more than its arm**: it sets `Behavior::Reproduce::seed_launch` -- zero in every shipped species -- through `SpeciesRegistry::set_param`, which writes one authored parameter by `ParamId` into the **live registry** rather than into a `.ron`. Every parameter sweep in this repo has until now had to edit a species file and rebuild, which is the `include_str!` trap that has produced whole invalid sweeps here (identical output across settings, because the prebuilt binary never read the file); a sweep through `set_param` cannot go stale, and it refuses outright when the write matches nothing. Measured on `herb`, 8 founders, 27,000 frames, three world seeds: a reach of 12 holds 500/465/481 columns of 512 against 398/454/396, and puts **91/92/88 plants more than fifteen columns from a founder against 66/69/61 -- +38%, up on 3 of 3 seeds** |
| `soil_drawdown` | **What does one plant do to the whole bed's water, and which door does the water leave by?** A horizontal profile along the rooting row, a vertical one down the middle, and the bed's total split into what the sun took and what the plant drank | Built for the owner's overlay report -- *the lab holds at 62% until a single plant goes in, then the surface dries outward to 18% and nothing germinates*. **Its three views exist to separate two stories that look identical on an overlay and want opposite fixes**: a *sink* shrinks the total, a *ratchet* holds the total and polarises the distribution to the ends of the range. Measured: a sink, and the attribution is the whole finding -- **lost 285,280 = sun 283,711 + plant 1,569**, so 99.4% was evaporation and the plant's own draw was flat from frame 4,000. **The attribution trick generalises to any water question here**: `evaporation::tick` credits `World::atmospheric_bank` and `plant::transpire` credits nothing, so bank growth is the sun's take and the rest of the shortfall is the plant's -- otherwise indistinguishable. `founders=0` is the control and is not optional: an empty bed must hold flat at field capacity (it does, to the unit, at every stop), and without it a drifting bed would be blamed on the plant. The horizontal profile is what shows a *travelling* front -- a single column cannot see distance from the plant, and a total cannot see direction; it also reproduced the owner's left-faster-than-right asymmetry, which is sweep order rather than physics |
| `labmass` | **Where does a dead plant's mass go?** A ledger over the lab's own bed through grow / die / rot, reporting what fraction of everything that died reached soil, what vanished, and what is stuck in a material with no way out | Built for the owner's question *"after a plant or tree dies does every part of it actually degrade to soil?"*, which nothing here kept a ledger for -- `crown_census` counts material by height, `litter_probe` says where litter comes to *rest*, neither follows a cohort through. **Measured, 12 seeds: ~9% to soil, ~33% locked in `deadwood`, ~55% gone.** Three things in it generalise past decay. **It caught its own instrument lying, and the mechanism is the reusable part**: `World::rotted_to_solid` counts every decay that left *a solid*, so the `deadleaf -> litter` step scores identically to `litter -> soil` and the raw total overstated the return **fourfold** (34% against 8%) -- the fix (`rotted_onward`) keys on whether the *product* itself decays, which is data rather than a material name, so any chain in this engine can be read the same way. **`yield=` makes the sensitivity control one command rather than one rebuild**: materials are `include_str!`d, so `DECAY_YIELD` is set from the argument before the first tick and echoed, and `yield=1` must report 100% with `to_nothing 0` -- without that arm a 5% return is indistinguishable from a channel that never fired. And **the plateau check is a worked example of a badly-conditioned guard**: stated as a bare 5% relative test on the pool it was strictest exactly where it had least to say (a 30-cell residue had to move by at most one), and failed every arm in which the pool had nearly drained; it needs the "or there is too little left to move the answer" clause to be about anything. `control=empty` is the specificity half and doubles as the oscillator check on the quantity actually read -- the mineral bed moves **exactly zero** cells over an empty run, so `CLAUDE.md`'s +/-1,700-cell water-cycle swing on `cells lost` does not reach this census. `cull=0 cullevery=N` runs the bed on its own life cycle under repeated disturbance instead of one forced cohort, which is what answers `soil-accumulation-and-the-carbon-cycle.md`'s own closing question about a second and third generation. Echoes its own parameters; `png=` writes the bed |
| `labbatch` | **How different are two chambers that differ only by their seed — and is the engine reproducible at all?** Runs a rack of `lab::scene::LabBox` copies headless on threads and prints every run's census plus the min/median/max and max:min ratio of each column | **Two arms, and neither means anything without the other**, which is the reusable part: `same` (N copies of one seed) must report spread **exactly zero** or the engine is not reproducible and no comparison between any two chambers is interpretable; `seeded` must report spread **> 0** or the seed never reached the copies and the rack is one run wearing N labels. `arm=both` runs them in that order because a failure in the cheap one invalidates everything after it. Measured 2026-08-31 on the shipped bed, 12 seeds at 9,000 frames: `same` is 0 on all six columns, `seeded` gives plants **2.42x**, plant cells **2.62x**, animals **3.12x**, seeds borne **2.53x** min-to-max — **so the seed alone moves the census by 2.4-3.1x with no true effect present, and that is the bar any real comparison has to clear**. Generalises past the lab: it is *"run N of this scene and report the order statistic"*, so `sweep=<bed field> values=a,b,c` turns it into a paired factorial — the seed varies with the **replicate** and never with the setting, so setting A's replicate 3 and setting B's replicate 3 are the same world apart from the knob (`divergence`'s rule). Refuses a `sweep=` naming a field `params::write_bed` does not have, rather than sweeping a constant. Echoes its own parameters and prints MB-per-world before it starts |
| `labperf` | **What the evolution lab's frame is being *asked* for, and which phase asked** — over the real `LabBox` at four populations (empty / plants / ants / both): awake chunks, the cells inside their expanded dirty rects, the rayon dispatches per tick, how many of those cells the tick actually changed, a per-material census of what is still moving, and a per-phase diff naming which phase wrote the cells that keep the box awake | Built for `Reports/evolution-lab-frame-cost-2026-09-01.md`, because `lab_cost phases=1` names the *till* and never the *spender*: in this bed the answer is always the CA sweep and the field, and neither is code anyone wrote — both are driven by how much of the box is dirty. **Three designs in it generalise past the lab.** The attribution is a **grid diff between phases**, not a counter, because `Chunk::mark_dirty` is called from inside `World::set` on a path every writer in the engine shares and `pending_dirty` is one rect per chunk — so by the end of a tick it records *that* a chunk was written and never *by whom*; snapshotting and diffing costs about a millisecond a phase and answers it outright, and the same trick works for any shared write path. The `est_bbox` / `est_rows` columns **price a proposed change before it is built** — both are computed from the cells the previous tick changed, `est_bbox` reproducing the *current* rule so it can be read against the measured `swept` as the control on its own estimate; that pairing is reusable for any "would a tighter X be worth it" question. And `arm=empty` is a **positive control that has to read zero** — a sealed box with nothing living in it must report no awake chunks and no writes in any phase, or the harness is measuring its own bookkeeping and every attribution under it is noise wearing a table  **Since 2026-09-02 `lab_cost` carries the moisture channel's own counters** (`sw seen` / `sw soil` / `sw chgd`), which is where the "is this a speed-up or a subtraction" question is answered for that change: a moisture pass that costs nothing because it stopped moving water and one that costs nothing because it only walks the cells that need it are the same timing and opposite findings |
| `labzoom` | **What the lab looks like at every zoom step, and how much of the screen is not the box** — the shipped `Renderer` driven at the real 512x320 viewport, `adjust_zoom` against `zoom_within` side by side, one row per step, with the void fraction and the world cell at the middle of the screen printed on each tile | Built for the owner's *"you can zoom out farther than the lab fullscreen"*, and the reason it had to exist is the reusable part: **`labshot` renders the box at the box's own size**, so the one thing this question is about -- what the *viewport* shows around a world smaller than it -- is not in its pictures at all, at any setting. Any question of the form *"what does the camera do at the edges of the world"* wants a harness at the viewport rather than at the world. Two numbers ride beside each tile because the picture cannot say either: **VOID** (the share of the screen outside the world's bounds) separates a tile that is three quarters black from a dark bed, which look alike at contact-sheet size, and **MID** (the world cell at the centre of the screen) is the whole of "the zoom stayed where I was looking" -- a claim no still can carry. It scales `ground_y` with `height` for `lab_resolution`'s recorded reason: at `height=640` with the shipped `ground_y` the soil sits in the top quarter and the sheet photographs a scene error rather than a view. Measured 2026-09-01 on a 1024x640 bed: the old control's widest step put **87%** of the screen outside the box, the new one **0%** |
| `labstats` | **Is anything in the box being born, and how deep have the generations got?** Runs `lab::scene::LabBox` and dumps the biosphere page (`src/lab/stats.rs`) as one greppable line per stop *and* as the page's own text, then renders it over the bed | **It is a control harness first and a renderer second**, because a page of counters over a living box is the exact shape `CLAUDE.md` says lies most readily. Six `control=` modes, each a box whose answer is known before the run: `empty` is the specificity half (nothing alive, every figure must be 0), `plants` and `ants` are each other's positive and negative halves in one box (a plants-only bed must report `animals 0` **and** `plants > 0`, or "no animals" is a test of the census being blind), `cull` is the sensitivity half nothing else gives — half the stand killed through `World::mark_organism_senescent` mid-run, and the strip must fall (measured: 26 killed, plants peak 14 → 3, plant biomass 394 → 3). `steady` is the **divide-out-the-oscillator check for any sealed box**, not just this page: it reads the same quantity at successive nominal day boundaries and again half a day later, and a systematic gap between the two rows is a cycle to remove. Measured on the standard bed, there is none — the lab holds its sky at noon and pins the weather clear, so the biggest oscillator in the engine is gone by construction and this is the run that says so rather than assuming it. `cost` times the census against the bare organism walk, paired and alternating in one process. **Generalises past the lab**: `cull` is a ready-made graded-die-back disturbance for anything that reads a population, and the page's text dump is the reason a 5x7 glyph in a downscaled screenshot never has to be guessed at again (`Reports/lanes/creature-lane-g.md` records an hour lost to a `3` one glyph wide). Echoes its own parameters; `zoom=` is a nearest-neighbour integer upscale for the review queue, `hover=` puts the page's own explanation of a row into the picture |

## The ones that generalise past what they were built for

**`divergence` is axis-agnostic, and this is the entry that stops it being
rebuilt.** Everything downstream of the axis — the two-world founder
construction, the exact-zero control, both metrics, the seed sweep, the
establishment-imbalance warning, the axis-survival check — does not know what
is being varied. **Adding an axis is one arm on `Axis` and nothing else.** It
therefore already answers:

- **Any single-axis morphology comparison.** `soil=`, `founders=`, `width=`,
  `species=`, `frames=` are parameters already, so "does soil depth change
  root:shoot", "does crowding change slenderness", "do two species differ in
  shape at the same size" are each a *run*, not a build.
- **Does a new genome locus move morphology at all?** Point it at two patches
  differing only in the locus and read the sign agreement. This is precisely
  the measurement `plant-species-authoring.md` §1 wanted when it found
  `light_weight` and `upward_weight` inert — and the measurement
  `plant-appearance-design.md` needed when three architectural levers all
  fired and moved no silhouette.
- **A determinism check, for free.** Its control asserts two identically built
  worlds diverge by *exactly* zero. A non-zero return on `control=1` means
  determinism has broken — which `PLAN.md` requires and which nothing else
  routinely exercises at whole-organism scale.

**`anchor_probe` is a *provenance* harness, and that is what generalises.**
It was built to ask whether `structural.rs`' three disagreeing anchor rules
matter, and everything downstream of "which function writes the field" is
indifferent to what the field is. Three properties worth reusing:

- **One geometry, N routes.** The obvious build — paint a structure, generate
  one, dig one — cannot answer the question, because the arms would differ in
  *shape* as well as in rule. Building once and writing the field several ways
  makes the scene a constant by construction, and the probe prints a material
  census per arm as the control that says so.
- **It sweeps for a margin, not an outcome.** Past its margin every rule
  agrees a structure falls; short of it every rule agrees it stands. A rule
  can only show itself in *where the margin is*, which is also the quantity a
  player feels — how far you can build before it comes down. The first run of
  this probe put the sand pile where the margin could not reach it and
  produced a null that said nothing about anything.
- **It prints the debug field beside the outcome.** That is what caught the
  two pointing opposite ways: under the brush's rule the deck reads as *better
  supported* (largest distance 9 against 82) and is the arm that collapses. A
  support overlay alone would have said the brush's field was the healthy one.

Reach for its shape for any "does this code path's version of X differ from
that one's" question — two writers of the same cached field, two builders of
the same state, a fast path against its slow reference.

**`arch_probe` is `anchor_probe`'s margin logic with the variable moved.**
`anchor_probe` holds one geometry and varies the *rule*; this holds one rule
and varies the *geometry*, and the transferable part is what sits between
them: **a comparison of two ways of doing something can only show itself in
where the margin is**, because on either side of it both arms agree. Three
things worth copying:

- **It refuses to report an unbracketed margin.** Its first sweep had every
  arm at 100% and it printed *"the sweep never reached its margin"* rather
  than a number. `anchor_probe`'s own first run produced exactly that null and
  did not say so.
- **It carries a control for each rival explanation, not just one.** The arch
  uses more stone than the lintel, so there is a cell-count-matched arm; and
  "it is really just depth" is a live alternative, so there is a
  triple-thickness arm. The second one *worked* (the margin went 56 → 96),
  which is what makes the arch's win over it meaningful.
- **It measures the scene rather than assuming it.** The clear span is read
  back off the built world as the widest empty run below the springing line,
  so two arms cannot silently be roofing different holes.

**`support_census` compares two fields by the *question their consumers ask*,
not by their values**, and that is the transferable part. `load.rs` never
reads a support distance as a magnitude — it reads four bits per cell, "which
of my neighbours are below me" — so two fields with wildly different numbers
can be identical to every rule downstream, and two fields with similar numbers
need not be. The census computes those bits under each field and diffs them.
Reach for the shape whenever a replacement is proposed for a cached quantity:
**diff the predicate the consumers evaluate, not the cache**.

Three things it does that are worth copying:

- **Two controls, opposite in sign.** A flat-zero field must read ~97%
  disagreement (the instrument can see a difference) and the exact field
  against itself must read exactly 100% same (it does not manufacture one). A
  set-comparison that is silently comparing a thing with itself reports
  perfect agreement, which is the answer a proposal wants.
- **It splits by whether anything ever looks.** At 8192x2560 only **10,344 of
  19.4 M** body cells pass `load::is_structurally_interesting`, so a
  whole-world agreement figure is 99.95% a statement about rock no rule
  evaluates. The headline and the meaningful number differed by 2.3x
  (50.49% against 21.82%).
- **It reports a chunk-boundary breakdown as a rate per band, never a
  count.** The interior band holds far more cells and wins on a count
  whatever the truth is; `CLAUDE.md`'s chunk-decomposition warning needs the
  rate to be checkable.

**`ant_ablation` and `pass_ablation` are the same idea in two domains**: turn
the mechanism off and see whether the outcome notices. Before concluding a
mechanism works, run the ablation — `CLAUDE.md`'s *a test can pass because the
code under it is dead* has an instrument, and this is it.

**`scale_probe phases=1` times a whole `App::update`, and `App::update` is not
a whole frame.** `Renderer::draw` is called from `main.rs`, not from
`App::update`, so **it is in none of these instruments and in no frame-cost
figure this repo has ever published** — including `frame-cost-audit-2026-08.md`,
whose headline is titled "where one frame actually goes". Measured 2026-08-29
on the shipped 8192x2560 world: `App::update` **18.9 ms** against a full
redraw of **~42 ms**, so the row nobody was measuring was the larger half by
2:1. (It is ~7.5 ms since, but the hole is structural and outlives the fix.)
A full redraw is not a worst case either — a camera move invalidates every
pixel, so it runs on ~100% of frames while the gnome walks.

**So quote two numbers or none.** `examples/render_cost` gives the second one;
`viewshot` prints its own redraw time per shot as a cheaper check. Anything
calling a `scale_probe` total "the frame" is quoting half of it, and this
index said so nowhere until now.

That aside, `scale_probe phases=1` is the only thing that times a whole
`App::update`, and it was built because nothing did. Every other cost figure
in this repo measures *part* of it, and the three that existed were taken at
three different world sizes — `ascii` times the CA sweep at 512x320,
`field_cost` the field at 8192x2560, `scale_probe`'s default mode the two
together. So "the field is the problem" was a reading off two numbers that had
never been placed beside the other nine phases. It runs `App::update`'s exact
order, times each phase, and buckets whole frames by sky-step and gust the way
`field_cost` does. Beyond the question it was built for it answers:

- **"Is this phase worth optimising?"** for any phase, since it prints each
  one's share. A phase at 2% cannot repay work whatever its internal cost.
- **"Does a new per-frame subsystem fit?"** — add it to the list and read its
  share against the 16.6 ms budget before it ships.
- **The idle cost of a loaded world**, which is what a player experiences most
  of the time and what M10's streaming has to hold down.

**A worldgen A/B needs no rebuild between arms, and that is worth knowing
before you build a bisect.** `assets/worldgen.ron` is read at runtime with
`std::fs::read_to_string`, while `assets/materials/*.ron` and
`assets/species/*.ron` are `include_str!`ed into the binary. So a sweep over
worldgen parameters is **one binary and N data files** — swap the file, re-run
the *same* executable — and the stale-example failure mode that
`CLAUDE.md` devotes four bullets to cannot occur at all. A materials or
species sweep is the exact opposite and has nothing *but* that hazard.

Used 2026-08-29 to bisect the owner's "the game feels slow" onto PR #94's
`sky_rows` in four arms with zero builds; the alternative was a
rebuild-at-every-point commit bisect whose own failure mode (identical numbers
across commits, read as "no regression") is silent. **Keep a positive control
on the swap** — the harness must print something world-dependent that moves
between arms, or the sweep is one world wearing N labels; `render_cost`'s
"visible pixels are empty sky" and `scale_probe`'s worldgen census lines both
serve.

**`ORGANISM_PASS=<every N>` splits `step_organisms` ten ways** (in
`plant.rs`, same shape as `FIELD_PASS`), and prints `live`/`ticked`/`cells`
beside the timings. The counters are the point: they are what said the cost is
per *cell ticked* rather than per live organism, which killed a plausible
optimisation before it was written. Reach for it for any "is this cost the
item count or the item size" question.

**It averages over a window, and that correction was worth 50x.** It printed
one *sampled* frame until 2026-09-03. Organisms tick on a stagger of
`ORGANISM_TICK_INTERVAL` (45), so any single frame holds ~1/45th of the
population and *which* 1/45th is a lottery: on the tree bed it caught **14
organisms holding 14 cells between them** and reported the pass at 0.08 ms
against a true 3.74. **Any profiler over a staggered schedule has this
shape** -- if the thing being sampled does not run every frame, one frame is
measuring the stagger. Its seven slots also missed `stress_field`,
`bend_under_load` and `break_under_load`, which is where the cost turned out
to be; it now covers all ten calls and the slots sum to within 0.02 ms of a
whole-function clock, so a gap between them is real and not uninstrumented
code.

**`ORGANISM_SIZE=<every N>` answers "is a big organism dear in proportion to
its size, or worse than that?"** -- it charges each organism's whole tick to a
geometric bucket by cell count and prints `us/tick` and **`us/cell`**. The
`us/cell` column is the one that decides whether a tail is worth attacking:
flat means linear and irreducible without per-cell work, rising means size is
being punished and there is an algorithmic win. Because ticks-per-organism
over a window is a known constant (`frames / 45`), **its `ticks` column is a
headcount** -- which is how §13 established that eleven trees are 96.6% of all
organism work and the other 676 organisms are 3.2%. Deliberately a separate
switch from `ORGANISM_PASS`: ten `Instant::now()` pairs per organism is a
per-*organism* overhead and would tilt the very curve being measured, taxing a
1-cell seed as hard as a 3,000-cell tree.

**`ORGANISM_PROLOGUE=<every N>` totals the nine cell-list rebuilds** one tick
pays -- the `collect()` + `sort_unstable_by_key` that opens `transport`,
`allocate_to_frontier`, `accumulate_support`, `anchor_support`,
`stress_field`, `break_root_tips`, `break_buds`, `organism_upkeep` and
`rot_remains`. Built to settle whether sharing one list across them is worth
the correctness surface; it is **not** (4.9%, against a ~15% estimate --
`dead-ends.md`). Generalises to any "this obviously-repeated thing must be
expensive" question, where the answer is usually that **repetition is not
magnitude**.

**`FIELD_PASS=<every N>` splits `field::step` ten ways**, and prints `solved`
and `momentum` beside the timings -- how many tiles the selective solve
actually visited, which is the number that says whether a field cost is "too
much work" or "too much per unit of work". **It averages over the window as of
2026-09-04**, and for this pass that matters more than for its siblings: the
field is driven by the sky, so a single sampled frame is a single *phase of a
designed oscillator*, and `frame % every == 0` pins the reading to whichever
phase it lands on. It is what established that §8's two handed-forward field
items are both stale (moisture is 0.006 ms; the selective solve visits 4% of
the box).

**`SCHED_PASS=<every N>` splits `scheduler::step` six ways**, one per
`ActiveKind`, and prints `sites` / `produced` / `deferred` beside the times.
Same shape as `FIELD_PASS` and `ORGANISM_PASS`, and the counters are again the
point:

- **`produced` against `sites` is a leak detector.** If a batch schedules as
  many sites as it drains, each site is replacing itself and the queue is
  self-sustaining however fast it is served -- which is how
  `open-bugs-handoff.md` §S was found (~8,100 produced against a 2,000 cap).
  Reach for it for any "does this backlog drain" question, not only a
  structural one.
- **`deferred` is the whole heap after the batch**, not the capped remainder,
  so it has a meaningful idle value (~5,400 at 8192x2560: ordinary
  future-dated growth and evaporation sites) and a drained queue is one that
  comes back to it.
- **`[struct]`'s second line attributes the structural share to a branch of
  `structural::tick`** -- `worsened` / `improved` / `unmoved`, plus the two
  defer reasons and the largest distance written. A `max aux` that keeps
  rising with the world's material dead still is the count-to-infinity
  dynamic and nothing else.

**`scale_probe load=` is what makes a cost question about a *verb*
answerable at all**, and it now carries all three destructive ones --
`blast:EVERY[:COUNT]`, `strike:EVERY[:COUNT]` (the hammer) and
`mine:EVERY[:COUNT]` (the pick), at the app's own `brush_radius`. Three
things it answers that were not the question it was built for:

- **`COUNT` separates "never drains" from "drains slower than it fills."**
  With uses still arriving the two look identical; fire a fixed number and
  watch the aftermath. This is what turned `open-bugs-handoff.md` §S from an
  explosion bug into a bug in every destructive verb but the brush.
- **Two verbs are a control on each other.** The hammer removes *fewer* cells
  than the pick and costs *more*, which rules out material-removed as the
  driver without needing a third measurement. Reach for a paired verb before
  reaching for a new metric.
- **`cells actually removed` is printed beside uses taken**, and that is not
  decoration. The first run of this probe reported **200 cuts and 0 cells
  removed** -- `rigid::is_tool_target` refuses `Powder`, and the probe was
  aiming at the topmost `Solid | Powder` cell, which on a rolling world is
  soil. The queue sat flat and it read exactly like "the pick is fine". A
  counter of *calls* is not a counter of *effect*; any new load component
  needs its own effect counter before its null means anything.

**`RECONVERGE_AT=<frame>` on `scale_probe` is the *oracle* for any question
of the form "would converging this help?"** It runs one whole-world
`compute_world_distances` mid-run and prints the pending count, the scheduler
cost and the census either side of it. Two uses well past the one it was
built for:

- **It separates "the reactive path is slow" from "the reactive path never
  arrives".** No amount of tuning a reactive relaxation can say what the
  converged state costs, because the converged state is what it never reaches.
  One pass says it directly: on `load=blast:200:1` the scheduler went 12.49 ms
  -> **0.25 ms** and pending 53,077 -> **6,094**, and stayed there. That is the
  measurement that made `Reports/structural-reconvergence-design.md` a scope
  worth building rather than a hypothesis.
- **It sizes the fix, because it censuses every body cell's `aux` either side
  of the pass.** "How much of the world does one charge actually invalidate?"
  is otherwise unanswerable, and the guess in circulation (250,000 cells,
  inferred from woken chunks) was **3.7x** the censused figure of 67,100.
  Run it on an idle world first: that arm reads **45 cells out of 19.4 M**,
  which is what makes the loaded arm's number mean anything.

It is a probe, not a proposal — the pass takes ~2,000 ms and walks all 21 M
cells. Nothing would ship it per blast.

**Put it on the *last* measured frame, not in the middle.** `step` counts the
measured frames only (`for step in 0..frames`), so a `RECONVERGE_AT` above
`frames=` never fires at all and prints nothing — which reads exactly like a
run where the oracle had nothing to say. And the pass itself converges the
field, so every frame after it is cheap: at `RECONVERGE_AT=frames-1` the
timing table is untouched and the census is still taken at the end of the run.

**`scale_probe band=N` sets how wide a strip the pick and hammer work
along, and without it no two world sizes are comparable.** The walk puts 64
cuts at a stride of `band / 64`, so the default 384 spans the same absolute
384 cells whatever the world is — **4.7% of an 8192-wide world and 19% of a
2048-wide one**. A size sweep run that way damages the small arm four times
harder than the large one and then compares absolute counts, which is how
`Reports/structural-support-model.md` §6.5d briefly acquired a third bug
that did not exist. Pass `band=$((w * 384 / 8192))` to hold the fraction
fixed.

**Its default owes a positive control and it is cheap to run**: at
`band=384` the 8192 arm must reproduce **4,490 wrong / 10,033 pending**
byte-for-byte. That check earned its keep immediately — an initial default
of 378 floored to a stride of **5** against the historical **6**, which
would have silently moved every recorded number in that report while looking
like a no-op.

**`scale_probe` echoes its own landing-aux arms** (`landing aux: settle=…
particle=…`) as of 2026-08-27, because `SETTLE_AUX` and `PARTICLE_AUX` decide
where §S's false anchors come from and a log that does not name them cannot be
told from one written before they existed. Each takes `zero|max|seed`. A run
whose header lacks that line came out of an older binary.

**`FALL=off` reverts the 2026-08-29 fall — the rate seeded from the break,
the tipping test on landing, and the centroid pivot — leaving everything else
alone**, so a paired run can hold the semantics fixed instead of comparing
two *binaries*. That distinction is what the switch exists for: every
instrument added alongside a mechanism is missing from the arm it is being
measured against, and the first reading of this change fell back on a
cluster-level statistic for exactly that reason. Verified against the
pre-change binary on `scene=fell`: every physics line identical.

**`filmstrip plants=0` is a bare bed, and it was unreachable until
2026-09-05** -- `plants` was a `usize` whose zero meant "not given", so the
argument silently resolved to 8 and the one control that separates *the
scene falling down on its own* from *the plants knocking it down* could not
be run. It is `Option<usize>` now. That control is what root-caused
`open-bugs-handoff.md` §W3 in a single run: over 24,000 frames a bare
`scene=grove` reports zero `overloaded` and zero `unsupported` failures and
`awake 0/40`, which proves the bed's 512-span stone slab is never evaluated
rather than sound. Reach for it whenever a scene misbehaves only once
something is living in it.

**`filmstrip channel=bend` draws the plant bending stress**, and its
quantitative pair is the `bending stress over N cells` line in the felling
census — median, peak, **where the peak is**, and how many cells read exactly
zero. Read them together: a ramp can only say "brighter", and whether the
hottest cell is at a trunk's base or out on a twig is the entire claim of the
model. **Not `channel=stress`**, which is `load::evaluate`'s rock stress and
a different quantity.

**The bend census is four lines and none of them substitutes for another.**
`cells leaning this tick` counts what *wants* to move (`|deflection| >= 1`);
`cells moved / refused` counts what did; `cross-sections blocked` and `would
have torn` split the refusals by *reason*, and that split is the one that can
be acted on. A grass stand once read **0 moved against 302 refused** — a
mechanism inert in a real world with every guard over it green — and the two
causes want opposite fixes. Blocked is a crowded stand doing its job; torn is
the one-piece rule turning a swing down because no cross-section could move
without stranding a cell. **Read them against `wind on that tissue`**, which
names the gust and the exposure range the moments were measured under: half
the moment now comes from the weather, so a lean count with no wind figure
beside it is that frame's phase plus the mechanism, inseparable.

**And the moment line is split per material**, because `stiffness` is a
per-material constant and one pooled distribution cannot fit two of them.
Trunks dominate a stand's pooled quantiles and foliage sits two orders of
magnitude below — leaf p90 260-348 under a stand p90 of 455-636 — so a
stiffness read off the pooled line is fitted to the wrong tissue. Each row
prints its own material's stiffness and how many of its cells want to lean,
which is the "did this constant connect to anything" reading.

**The break census is three lines, and the third is the one that fits the
constant.** `snapped under load` is the "did it fire" counter, kept separate
from `severed_organism_cells` because that already folds in limbs that lost
an anchor and cells that over-reached their span — a snap hidden inside it is
invisible. The per-material `stress` line says how many cells are over their
`strength`. And **`peak wood stress per plant`** is the one to fit against: a
*stand* maximum is one cell in eight thousand belonging to the single
worst-grown tree in the wood, so a threshold set above it is set above the
whole failure mode. That mistake was made, shipped a constant of 60,000 that
could not break anything even in a gale, and passed its own negative control
while doing it. The per-plant list is printed raw beside the quantiles
because a quantile over eight plants is not a quantile — p90 and max are the
same order statistic at that sample size — so pooling the raw lists across
seeds is what gives the tail enough plants to be a tail.

**`BREAK=off` holds every plant unbreakable by load**, the sibling of
`BEND=off` below and used the same way. It has a second use the bend switch
does not: it is how the fitting census is taken at all, since a strength
being *measured* must not be breaking the stand it is measured on.

**`BEND=off` holds every plant rigid** and is the control for all of the
above. It exists because both errors in this mechanism were found by holding
the semantics fixed and changing nothing else. Comparing two *binaries*
cannot do it: the counters that catch the error are added alongside the
mechanism, so the arm being measured against does not have them. Note the two
arms diverge — the sim differs — so a difference read thousands of frames
apart is two different worlds, not a measurement of the mechanism.

**`HINGE_PROBE=1` prints the felling hinge's own arithmetic** — the region's
size and mass, the stump it pivots about, `broke_at` beside it, the centre of
mass **as a vector from the pivot**, the second moment and `alpha`. Read the
vector, not `alpha`: a hinge whose centre of mass is level with its pivot
swings straight *down*, which on a contact sheet is indistinguishable from
the piece simply falling. That is not hypothetical — it is what the first
build did, for a whole render and a wrong reading, and no image could have
told anyone.

**`filmstrip`'s "foliage by steps to the nearest wood"** is a multi-source
BFS out of every woody cell, crossing only foliage, so bucket 1 is exactly
the set `update::on_a_branch` holds. It exists because "the leaves fall off
the branch" and "the rule that holds them is one cell too shallow" produce
the identical picture; the histogram separates them, and it is what set the
clinging depth (36% at one step, 53% at two). `no path` is foliage with no
route to wood through its own kind — already run clear, and no depth
recovers it.

**Two orientation censuses on `scene=fell`, and they answer different
questions.** `settled log pieces` folds settled `log` into 8-connected
clusters, so two logs that land touching are one "piece" whose orientation is
the *pile's*; its own doc records the largest "piece" going from 49x48 to
99x71 purely because the pile packed tighter. `how pieces came to rest`
(2026-08-29) is asked of each body as it lands, once, at the only moment its
extent is unambiguous. Reach for the second whenever the question is about
the pieces and the first when it is about the pile — and note that a change
which packs the pile tighter moves the first one *against* itself.

**The `[struct]` census is a *per-frame sample*, not a total, and that makes
`grounded` a one-way instrument.** `scheduler::step` drains
`structural::take_tick_census()` every frame and prints only on reporting
frames — deliberately, since a running total wearing a per-frame label would
be worse — and the line is further gated on that frame having done work. So a
counter in it answers "how many fired on *this* frame", and on a scene where
the mechanism is rare it prints **no line at all**: measured 2026-08-27, the
hammer arm at `SCHED_PASS=20` over 600 frames produced zero `[struct]` lines
on both arms of an A/B whose oracle counts differed 26-fold.

Read it accordingly: **a non-zero reading is real evidence the mechanism
fires; a zero is nearly none.** That was written on 2026-08-27 and then
immediately violated by its own author: "`acceptance` cannot see the ground
root, `grounded` reads 0 through `rockdrop` and `ligament`" went into two
documents before anyone tested it properly. **The test that works is
differential** -- run the suite with the rule at both values and diff
everything that is not a wall clock. Three of the 22 cases turned out to see
it, and `scene=strike` turned out to be §S in miniature. See
`Reports/structural-support-model.md` §6.5e. `Reports/structural-support-model.md` §6.5's
"`grounded` reads 0 on every frame, so the ablation is vacuous" stands on the
byte-identical output beside it rather than on the counter. When the question
is "how much is left", use a whole-world census instead — `RECONVERGE_AT`'s
`of changed, was at 0 (tick ground-root)` is the one that answers it for this
particular rule.

**`filmstrip`'s `max_sites=N` asserts that the structural scheduler
*drains*, and it is the one gate in the suite that states
`open-bugs-handoff.md` §S directly.** A **final** count rather than a peak,
deliberately: §S is "pinned at its cap for ever", so the refutation is not
that the backlog stays small -- a real blow should spike it -- but that it
comes back down. On `scene=strike` the shipped engine reads 958 / 968 / 824
/ **289** across frames 2-182 and a reverted one reads 958 / 2747 / 5034 /
**7145**, so only the last reading separates them by more than 3x.

A counter and not a wall clock, which is why it can be trusted where this
suite's two `max_frame_ms` gates cannot: those flaked twice on a loaded box
inside one session, and this one is bit-identical under any load.

**`AUX_TRAP` is a *write-seam trap*, and the shape is the reusable part.**
It is not an example — it is an env-gated report inside `World::set` that
fires on any write matching a predicate and prints a backtrace. Reach for it
when you know *what* wrong state exists and not *who* wrote it, and when
guessing writers by name has already failed twice: `Reports/structural-support-
model.md` §6 ablated `rigid::settle` and `structural::tick`'s `grounded_root`
before trapping the seam, and the answer was neither (`particle.rs::
landed_cell`, twelve backtraces out of twelve). Three things that generalise:

- **Trap the invariant, not the caller.** `World::set`'s own doc already
  states the principle for a different problem — *"an enumeration that has to
  stay complete is the failure mode this project keeps rediscovering"* — and
  a predicate over (old cell, new cell, position) is complete by construction
  where a list of suspects is not.
- **State the predicate as the bug, not as the symptom.** Here: *this write
  makes a cell body material reading `aux <= 2` with no bedrock adjacent, and
  the cell it replaced was nowhere near an anchor.* That is "a false anchor is
  being created" in one line, and it fired 12 times in two frames with no
  false positives.
- **Cap the reports and print the neighbourhood.** The cap keeps the first
  report readable through a cascade; the neighbourhood is what showed the
  error spreading — the first traps sit beside `stone:2405` and the last
  beside `stone:0`.

**`flora_census where=/at=` is the answer to "I don't see a difference".**
Audit the rendered window before believing a card; a whole-world total in a
card's `meta` cannot say whether the thing is even in frame.
